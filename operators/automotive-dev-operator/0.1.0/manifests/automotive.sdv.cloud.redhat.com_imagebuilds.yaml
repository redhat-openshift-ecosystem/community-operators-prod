apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  name: imagebuilds.automotive.sdv.cloud.redhat.com
spec:
  group: automotive.sdv.cloud.redhat.com
  names:
    kind: ImageBuild
    listKind: ImageBuildList
    plural: imagebuilds
    singular: imagebuild
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ImageBuild is the Schema for the imagebuilds API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ImageBuildSpec defines the desired state of ImageBuild
            properties:
              aib:
                description: AIB contains automotive-image-builder specific configuration
                properties:
                  builderImage:
                    description: |-
                      BuilderImage specifies a custom osbuild builder container image
                      If not specified for bootc builds, one is automatically built and cached
                    type: string
                  containerRef:
                    description: |-
                      ContainerRef is the reference to an existing bootc container image
                      Required when mode=disk to create a disk image from an existing container
                    type: string
                  distro:
                    description: Distro specifies the distribution to build for (e.g.,
                      "autosd")
                    type: string
                  image:
                    description: |-
                      Image specifies the automotive-image-builder container image to use
                      If not specified, the default from OperatorConfig is used
                    type: string
                  inputFilesServer:
                    description: |-
                      InputFilesServer indicates if an upload server should be created for local file references
                      When true, the build waits in "Uploading" phase until files are uploaded
                    type: boolean
                  manifestConfigMap:
                    description: ManifestConfigMap specifies the name of the ConfigMap
                      containing the AIB manifest
                    type: string
                  mode:
                    default: image
                    description: Mode specifies the build mode
                    enum:
                    - package
                    - image
                    - bootc
                    - disk
                    type: string
                  target:
                    description: Target specifies the build target platform (e.g.,
                      "qemu", "aws")
                    type: string
                required:
                - distro
                - target
                type: object
              architecture:
                description: Architecture specifies the target architecture (e.g.,
                  "amd64", "arm64")
                type: string
              export:
                description: Export contains configuration for exporting build artifacts
                properties:
                  buildDiskImage:
                    description: BuildDiskImage indicates whether to build a disk
                      image from the bootc container
                    type: boolean
                  compression:
                    default: gzip
                    description: Compression specifies the compression algorithm for
                      artifacts
                    enum:
                    - lz4
                    - gzip
                    - xz
                    type: string
                  container:
                    description: Container is the OCI registry URL to push the bootc
                      container image
                    type: string
                  disk:
                    description: Disk contains configuration for disk image export
                    properties:
                      oci:
                        description: OCI is the registry URL to push the disk image
                          as an OCI artifact
                        type: string
                    type: object
                  format:
                    default: qcow2
                    description: Format specifies the disk image output format (e.g.,
                      raw, qcow2, simg, or any AIB-supported format)
                    type: string
                  useServiceAccountAuth:
                    description: |-
                      UseServiceAccountAuth indicates the build should authenticate to the registry
                      using a service account token instead of explicit credentials
                    type: boolean
                type: object
              flash:
                description: Flash contains configuration for flashing the built image
                  to hardware via Jumpstarter
                properties:
                  clientConfigSecretRef:
                    description: |-
                      ClientConfigSecretRef is the name of the secret containing the Jumpstarter client config
                      The secret should have a key "client.yaml" with the config contents
                      If set, flash is enabled automatically
                    type: string
                  leaseDuration:
                    default: "03:00:00"
                    description: LeaseDuration is the duration for the device lease
                      in HH:MM:SS format
                    type: string
                type: object
              pushSecretRef:
                description: |-
                  PushSecretRef is the name of the kubernetes.io/dockerconfigjson secret for pushing artifacts
                  This is separate from SecretRef because push operations require docker config format
                type: string
              runtimeClassName:
                description: RuntimeClassName specifies the runtime class to use for
                  the build pod
                type: string
              secretRef:
                description: |-
                  SecretRef is the name of the secret containing credentials for registry operations
                  The secret should contain keys like REGISTRY_AUTH_FILE for authentication
                type: string
              storageClass:
                description: StorageClass is the name of the storage class to use
                  for the build PVC
                type: string
            type: object
          status:
            description: ImageBuildStatus defines the observed state of ImageBuild
            properties:
              aibImageUsed:
                description: AIBImageUsed is the automotive-image-builder container
                  image that was used for the build
                type: string
              builderImageUsed:
                description: |-
                  BuilderImageUsed is the osbuild builder container image that was used for the build
                  This is particularly useful for bootc builds where the builder may be auto-generated
                type: string
              completionTime:
                description: CompletionTime is when the build finished
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the ImageBuild's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              flashTaskRunName:
                description: FlashTaskRunName is the name of the TaskRun for flashing
                  to hardware
                type: string
              leaseId:
                description: LeaseID is the Jumpstarter lease ID acquired during flash
                type: string
              message:
                description: Message provides more detail about the current phase
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the build (Building,
                  Completed, Failed)
                enum:
                - Pending
                - Uploading
                - Building
                - Pushing
                - Flashing
                - Completed
                - Failed
                type: string
              pipelineRunName:
                description: PipelineRunName is the name of the active PipelineRun
                  for this build
                type: string
              pushTaskRunName:
                description: PushTaskRunName is the name of the TaskRun for pushing
                  artifacts to registry
                type: string
              pvcName:
                description: PVCName is the name of the PVC where the artifact is
                  stored
                type: string
              startTime:
                description: StartTime is when the build started
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
