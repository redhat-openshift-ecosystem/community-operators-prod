apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: konfluxes.konflux.konflux-ci.dev
spec:
  group: konflux.konflux-ci.dev
  names:
    kind: Konflux
    listKind: KonfluxList
    plural: konfluxes
    singular: konflux
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Ready status
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: URL to access the Konflux UI
      jsonPath: .status.uiURL
      name: UI-URL
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Konflux is the Schema for the konfluxes API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KonfluxSpec defines the desired state of Konflux.
            properties:
              buildService:
                description: |-
                  KonfluxBuildService configures the build-service component.
                  The runtime configuration is copied to the KonfluxBuildService CR by the operator.
                properties:
                  spec:
                    description: Spec configures the build-service component (excludes
                      componentMetrics; see spec.componentMetrics).
                    properties:
                      buildControllerManager:
                        description: BuildControllerManager defines customizations
                          for the controller-manager deployment.
                        properties:
                          manager:
                            description: Manager defines customizations for the manager
                              container.
                            properties:
                              env:
                                description: Env specifies environment variables for
                                  the container.
                                items:
                                  description: EnvVar represents an environment variable
                                    present in a Container.
                                  properties:
                                    name:
                                      description: |-
                                        Name of the environment variable.
                                        May consist of any printable ASCII characters except '='.
                                      type: string
                                    value:
                                      description: |-
                                        Variable references $(VAR_NAME) are expanded
                                        using the previously defined environment variables in the container and
                                        any service environment variables. If a variable cannot be resolved,
                                        the reference in the input string will be unchanged. Double $$ are reduced
                                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                        Escaped references will never be expanded, regardless of whether the variable
                                        exists or not.
                                        Defaults to "".
                                      type: string
                                    valueFrom:
                                      description: Source for the environment variable's
                                        value. Cannot be used if value is not empty.
                                      properties:
                                        configMapKeyRef:
                                          description: Selects a key of a ConfigMap.
                                          properties:
                                            key:
                                              description: The key to select.
                                              type: string
                                            name:
                                              default: ""
                                              description: |-
                                                Name of the referent.
                                                This field is effectively required, but due to backwards compatibility is
                                                allowed to be empty. Instances of this type with an empty value here are
                                                almost certainly wrong.
                                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                              type: string
                                            optional:
                                              description: Specify whether the ConfigMap
                                                or its key must be defined
                                              type: boolean
                                          required:
                                          - key
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        fieldRef:
                                          description: |-
                                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                          properties:
                                            apiVersion:
                                              description: Version of the schema the
                                                FieldPath is written in terms of,
                                                defaults to "v1".
                                              type: string
                                            fieldPath:
                                              description: Path of the field to select
                                                in the specified API version.
                                              type: string
                                          required:
                                          - fieldPath
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        fileKeyRef:
                                          description: |-
                                            FileKeyRef selects a key of the env file.
                                            Requires the EnvFiles feature gate to be enabled.
                                          properties:
                                            key:
                                              description: |-
                                                The key within the env file. An invalid key will prevent the pod from starting.
                                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                              type: string
                                            optional:
                                              default: false
                                              description: |-
                                                Specify whether the file or its key must be defined. If the file or key
                                                does not exist, then the env var is not published.
                                                If optional is set to true and the specified key does not exist,
                                                the environment variable will not be set in the Pod's containers.

                                                If optional is set to false and the specified key does not exist,
                                                an error will be returned during Pod creation.
                                              type: boolean
                                            path:
                                              description: |-
                                                The path within the volume from which to select the file.
                                                Must be relative and may not contain the '..' path or start with '..'.
                                              type: string
                                            volumeName:
                                              description: The name of the volume
                                                mount containing the env file.
                                              type: string
                                          required:
                                          - key
                                          - path
                                          - volumeName
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        resourceFieldRef:
                                          description: |-
                                            Selects a resource of the container: only resources limits and requests
                                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                          properties:
                                            containerName:
                                              description: 'Container name: required
                                                for volumes, optional for env vars'
                                              type: string
                                            divisor:
                                              anyOf:
                                              - type: integer
                                              - type: string
                                              description: Specifies the output format
                                                of the exposed resources, defaults
                                                to "1"
                                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                              x-kubernetes-int-or-string: true
                                            resource:
                                              description: 'Required: resource to
                                                select'
                                              type: string
                                          required:
                                          - resource
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        secretKeyRef:
                                          description: Selects a key of a secret in
                                            the pod's namespace
                                          properties:
                                            key:
                                              description: The key of the secret to
                                                select from.  Must be a valid secret
                                                key.
                                              type: string
                                            name:
                                              default: ""
                                              description: |-
                                                Name of the referent.
                                                This field is effectively required, but due to backwards compatibility is
                                                allowed to be empty. Instances of this type with an empty value here are
                                                almost certainly wrong.
                                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                              type: string
                                            optional:
                                              description: Specify whether the Secret
                                                or its key must be defined
                                              type: boolean
                                          required:
                                          - key
                                          type: object
                                          x-kubernetes-map-type: atomic
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              resources:
                                description: Resources specifies the resource requirements
                                  for the container.
                                properties:
                                  claims:
                                    description: |-
                                      Claims lists the names of resources, defined in spec.resourceClaims,
                                      that are used by this container.

                                      This field depends on the
                                      DynamicResourceAllocation feature gate.

                                      This field is immutable. It can only be set for containers.
                                    items:
                                      description: ResourceClaim references one entry
                                        in PodSpec.ResourceClaims.
                                      properties:
                                        name:
                                          description: |-
                                            Name must match the name of one entry in pod.spec.resourceClaims of
                                            the Pod where this field is used. It makes that resource available
                                            inside a container.
                                          type: string
                                        request:
                                          description: |-
                                            Request is the name chosen for a request in the referenced claim.
                                            If empty, everything from the claim is made available, otherwise
                                            only the result of this request.
                                          type: string
                                      required:
                                      - name
                                      type: object
                                    type: array
                                    x-kubernetes-list-map-keys:
                                    - name
                                    x-kubernetes-list-type: map
                                  limits:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: |-
                                      Limits describes the maximum amount of compute resources allowed.
                                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    type: object
                                  requests:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: |-
                                      Requests describes the minimum amount of compute resources required.
                                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    type: object
                                type: object
                            type: object
                          replicas:
                            default: 1
                            description: Replicas is the number of replicas for the
                              controller-manager deployment.
                            format: int32
                            minimum: 1
                            type: integer
                        type: object
                      logEncoder:
                        description: |-
                          LogEncoder sets the log encoding format for the build-service controller.
                          When not set, the upstream default (json) is used.
                        enum:
                        - json
                        - console
                        type: string
                      pacWebhookInsecureSSL:
                        description: |-
                          PACWebhookInsecureSSL controls TLS certificate verification when the build-service
                          configures webhooks on git providers.
                          When set to true, sets PAC_WEBHOOK_INSECURE_SSL=1 on the controller
                          (use only in development or test environments with self-signed certificates).
                          When set to false, forces PAC_WEBHOOK_INSECURE_SSL off, overriding any
                          value in buildControllerManager.manager.env.
                          When omitted, the upstream default applies and PAC_WEBHOOK_INSECURE_SSL
                          in buildControllerManager.manager.env (if set) takes effect.
                        type: boolean
                      pipelineConfig:
                        description: |-
                          PipelineConfig controls the contents of the build-pipeline-config ConfigMap.
                          The operator always manages this ConfigMap; use this field to customize which
                          pipelines are included.

                          Default behavior (nil or empty):
                            The operator applies the full set of default pipeline bundle references.

                          Merge behavior:
                            When set, the operator merges user-specified pipelines with the defaults.
                            Pipelines with matching names override the defaults.
                            Pipelines with removed: true exclude the matching default.
                            Set removeDefaults: true to discard all defaults and use only user-specified pipelines.
                        properties:
                          defaultPipelineName:
                            description: |-
                              DefaultPipelineName specifies which pipeline to use as the default.
                              The referenced pipeline must exist in the final merged pipeline list.
                              When not set, the operator-provided default is preserved.
                            maxLength: 253
                            minLength: 1
                            type: string
                          pipelines:
                            description: |-
                              Pipelines specifies user-provided pipeline overrides or additions.
                              Entries with matching names override operator defaults.
                            items:
                              description: PipelineSpec defines a single pipeline
                                entry in the build-pipeline-config ConfigMap.
                              properties:
                                bundle:
                                  description: Bundle is the Tekton bundle reference
                                    for this pipeline.
                                  type: string
                                description:
                                  description: |-
                                    Description is a human-readable description of this pipeline.
                                    When overriding a default pipeline, this replaces the default description.
                                  type: string
                                name:
                                  description: Name is the pipeline identifier. Must
                                    match a default pipeline name to override it.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                removed:
                                  description: |-
                                    Removed excludes this pipeline from the final configuration.
                                    Use to remove a specific operator-provided default pipeline.
                                    When true, bundle must not be set.
                                  type: boolean
                              required:
                              - name
                              type: object
                              x-kubernetes-validations:
                              - message: bundle must not be set when removed is true
                                rule: '!has(self.bundle) || !has(self.removed) ||
                                  !self.removed'
                              - message: bundle is required when removed is not true
                                rule: (has(self.removed) && self.removed) || (has(self.bundle)
                                  && size(self.bundle) > 0)
                            maxItems: 64
                            type: array
                            x-kubernetes-list-type: atomic
                          removeDefaults:
                            description: |-
                              RemoveDefaults disables all operator-provided default pipelines.
                              When true, only user-specified pipelines in the Pipelines list are included.
                            type: boolean
                        type: object
                        x-kubernetes-map-type: atomic
                        x-kubernetes-validations:
                        - message: defaultPipelineName is required when removeDefaults
                            is true
                          rule: '!has(self.removeDefaults) || !self.removeDefaults
                            || has(self.defaultPipelineName)'
                        - message: at least one pipeline with a bundle must be provided
                            when removeDefaults is true
                          rule: '!has(self.removeDefaults) || !self.removeDefaults
                            || self.pipelines.exists(p, !has(p.removed) || !p.removed)'
                        - message: defaultPipelineName must reference a pipeline in
                            the pipelines list when removeDefaults is true
                          rule: '!has(self.removeDefaults) || !self.removeDefaults
                            || !has(self.defaultPipelineName) || self.pipelines.exists(p,
                            p.name == self.defaultPipelineName && (!has(p.removed)
                            || !p.removed))'
                        - message: defaultPipelineName must not reference a pipeline
                            that is being removed
                          rule: '!has(self.defaultPipelineName) || !self.pipelines.exists(p,
                            p.name == self.defaultPipelineName && has(p.removed) &&
                            p.removed)'
                      webhookURLs:
                        additionalProperties:
                          type: string
                        description: |-
                          WebhookURLs maps repository URL prefixes to externally-reachable webhook URLs.
                          When configured, the build-service uses these URLs instead of the default PaC webhook
                          URL when setting up webhooks for git repositories.

                          Keys are matched against the full repository URL using longest-prefix matching.
                          An empty string key ("") serves as a catch-all default for any unmatched repository.

                          Example:
                            webhookURLs:
                              "https://github.com": "https://smee.example.com/github-hook"
                              "https://gitlab.com": "https://smee.example.com/gitlab-hook"
                        type: object
                    type: object
                type: object
              certManager:
                description: |-
                  CertManager configures the cert-manager component.
                  The runtime configuration is copied to the KonfluxCertManager CR by the operator.
                properties:
                  createClusterIssuer:
                    description: |-
                      CreateClusterIssuer controls whether cluster issuer resources are created.
                      Defaults to true if not specified.
                    type: boolean
                type: object
              componentMetrics:
                description: |-
                  ComponentMetrics controls Prometheus scrape resources for metrics-enabled components
                  (see operator/docs/component-monitoring.md#scope). When disabled, those reconcilers
                  do not apply ServiceMonitor, metrics-reader RBAC, or operand scrape-token resources.
                properties:
                  enabled:
                    description: |-
                      Enabled controls whether scrape resources are deployed for metrics-enabled components
                      (see operator/docs/component-monitoring.md#scope). HTTPS operands on the operator
                      scrape-token model use reconciler-managed prometheus-scrape-token; legacy interim
                      operands use static-token HTTP scraping until migrated.
                      Defaults to true when unset.
                    type: boolean
                type: object
              defaultTenant:
                description: |-
                  DefaultTenant configures the default tenant component.
                  The default tenant provides a namespace accessible by all authenticated users.
                  The runtime configuration is copied to the KonfluxDefaultTenant CR by the operator.
                properties:
                  enabled:
                    description: |-
                      Enabled controls whether the default tenant is created.
                      Defaults to true if not specified.
                    type: boolean
                type: object
              enterpriseContract:
                description: |-
                  EnterpriseContract configures the enterprise-contract component.
                  The runtime configuration is copied to the KonfluxEnterpriseContract CR by the operator.
                properties:
                  skipPolicies:
                    description: |-
                      SkipPolicies disables deployment of EnterpriseContractPolicy resources.
                      When true, only CRDs, namespace, RBAC, and ConfigMap are deployed;
                      users are expected to manage policies externally.
                    type: boolean
                type: object
              imageController:
                description: |-
                  ImageController configures the image-controller component.
                  The runtime configuration is copied to the KonfluxImageController CR by the operator.
                properties:
                  enabled:
                    description: |-
                      Enabled indicates whether image-controller should be deployed.
                      If false or unset, the component CR will not be created.
                    type: boolean
                  spec:
                    description: Spec configures the image-controller component (excludes
                      componentMetrics; see spec.componentMetrics).
                    properties:
                      imageControllerManager:
                        description: ImageControllerManager defines customizations
                          for the controller-manager deployment.
                        properties:
                          manager:
                            description: Manager defines customizations for the manager
                              container.
                            properties:
                              env:
                                description: Env specifies environment variables for
                                  the container.
                                items:
                                  description: EnvVar represents an environment variable
                                    present in a Container.
                                  properties:
                                    name:
                                      description: |-
                                        Name of the environment variable.
                                        May consist of any printable ASCII characters except '='.
                                      type: string
                                    value:
                                      description: |-
                                        Variable references $(VAR_NAME) are expanded
                                        using the previously defined environment variables in the container and
                                        any service environment variables. If a variable cannot be resolved,
                                        the reference in the input string will be unchanged. Double $$ are reduced
                                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                        Escaped references will never be expanded, regardless of whether the variable
                                        exists or not.
                                        Defaults to "".
                                      type: string
                                    valueFrom:
                                      description: Source for the environment variable's
                                        value. Cannot be used if value is not empty.
                                      properties:
                                        configMapKeyRef:
                                          description: Selects a key of a ConfigMap.
                                          properties:
                                            key:
                                              description: The key to select.
                                              type: string
                                            name:
                                              default: ""
                                              description: |-
                                                Name of the referent.
                                                This field is effectively required, but due to backwards compatibility is
                                                allowed to be empty. Instances of this type with an empty value here are
                                                almost certainly wrong.
                                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                              type: string
                                            optional:
                                              description: Specify whether the ConfigMap
                                                or its key must be defined
                                              type: boolean
                                          required:
                                          - key
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        fieldRef:
                                          description: |-
                                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                          properties:
                                            apiVersion:
                                              description: Version of the schema the
                                                FieldPath is written in terms of,
                                                defaults to "v1".
                                              type: string
                                            fieldPath:
                                              description: Path of the field to select
                                                in the specified API version.
                                              type: string
                                          required:
                                          - fieldPath
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        fileKeyRef:
                                          description: |-
                                            FileKeyRef selects a key of the env file.
                                            Requires the EnvFiles feature gate to be enabled.
                                          properties:
                                            key:
                                              description: |-
                                                The key within the env file. An invalid key will prevent the pod from starting.
                                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                              type: string
                                            optional:
                                              default: false
                                              description: |-
                                                Specify whether the file or its key must be defined. If the file or key
                                                does not exist, then the env var is not published.
                                                If optional is set to true and the specified key does not exist,
                                                the environment variable will not be set in the Pod's containers.

                                                If optional is set to false and the specified key does not exist,
                                                an error will be returned during Pod creation.
                                              type: boolean
                                            path:
                                              description: |-
                                                The path within the volume from which to select the file.
                                                Must be relative and may not contain the '..' path or start with '..'.
                                              type: string
                                            volumeName:
                                              description: The name of the volume
                                                mount containing the env file.
                                              type: string
                                          required:
                                          - key
                                          - path
                                          - volumeName
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        resourceFieldRef:
                                          description: |-
                                            Selects a resource of the container: only resources limits and requests
                                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                          properties:
                                            containerName:
                                              description: 'Container name: required
                                                for volumes, optional for env vars'
                                              type: string
                                            divisor:
                                              anyOf:
                                              - type: integer
                                              - type: string
                                              description: Specifies the output format
                                                of the exposed resources, defaults
                                                to "1"
                                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                              x-kubernetes-int-or-string: true
                                            resource:
                                              description: 'Required: resource to
                                                select'
                                              type: string
                                          required:
                                          - resource
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        secretKeyRef:
                                          description: Selects a key of a secret in
                                            the pod's namespace
                                          properties:
                                            key:
                                              description: The key of the secret to
                                                select from.  Must be a valid secret
                                                key.
                                              type: string
                                            name:
                                              default: ""
                                              description: |-
                                                Name of the referent.
                                                This field is effectively required, but due to backwards compatibility is
                                                allowed to be empty. Instances of this type with an empty value here are
                                                almost certainly wrong.
                                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                              type: string
                                            optional:
                                              description: Specify whether the Secret
                                                or its key must be defined
                                              type: boolean
                                          required:
                                          - key
                                          type: object
                                          x-kubernetes-map-type: atomic
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              resources:
                                description: Resources specifies the resource requirements
                                  for the container.
                                properties:
                                  claims:
                                    description: |-
                                      Claims lists the names of resources, defined in spec.resourceClaims,
                                      that are used by this container.

                                      This field depends on the
                                      DynamicResourceAllocation feature gate.

                                      This field is immutable. It can only be set for containers.
                                    items:
                                      description: ResourceClaim references one entry
                                        in PodSpec.ResourceClaims.
                                      properties:
                                        name:
                                          description: |-
                                            Name must match the name of one entry in pod.spec.resourceClaims of
                                            the Pod where this field is used. It makes that resource available
                                            inside a container.
                                          type: string
                                        request:
                                          description: |-
                                            Request is the name chosen for a request in the referenced claim.
                                            If empty, everything from the claim is made available, otherwise
                                            only the result of this request.
                                          type: string
                                      required:
                                      - name
                                      type: object
                                    type: array
                                    x-kubernetes-list-map-keys:
                                    - name
                                    x-kubernetes-list-type: map
                                  limits:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: |-
                                      Limits describes the maximum amount of compute resources allowed.
                                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    type: object
                                  requests:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: |-
                                      Requests describes the minimum amount of compute resources required.
                                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    type: object
                                type: object
                            type: object
                          replicas:
                            default: 1
                            description: Replicas is the number of replicas for the
                              controller-manager deployment.
                            format: int32
                            minimum: 1
                            type: integer
                        type: object
                      imagePruner:
                        description: |-
                          ImagePruner defines customizations for the image-pruner CronJob container
                          (resources, env vars). When not set, the upstream defaults apply.
                        properties:
                          env:
                            description: Env specifies environment variables for the
                              container.
                            items:
                              description: EnvVar represents an environment variable
                                present in a Container.
                              properties:
                                name:
                                  description: |-
                                    Name of the environment variable.
                                    May consist of any printable ASCII characters except '='.
                                  type: string
                                value:
                                  description: |-
                                    Variable references $(VAR_NAME) are expanded
                                    using the previously defined environment variables in the container and
                                    any service environment variables. If a variable cannot be resolved,
                                    the reference in the input string will be unchanged. Double $$ are reduced
                                    to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                    "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                    Escaped references will never be expanded, regardless of whether the variable
                                    exists or not.
                                    Defaults to "".
                                  type: string
                                valueFrom:
                                  description: Source for the environment variable's
                                    value. Cannot be used if value is not empty.
                                  properties:
                                    configMapKeyRef:
                                      description: Selects a key of a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    fieldRef:
                                      description: |-
                                        Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                        spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                      properties:
                                        apiVersion:
                                          description: Version of the schema the FieldPath
                                            is written in terms of, defaults to "v1".
                                          type: string
                                        fieldPath:
                                          description: Path of the field to select
                                            in the specified API version.
                                          type: string
                                      required:
                                      - fieldPath
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    fileKeyRef:
                                      description: |-
                                        FileKeyRef selects a key of the env file.
                                        Requires the EnvFiles feature gate to be enabled.
                                      properties:
                                        key:
                                          description: |-
                                            The key within the env file. An invalid key will prevent the pod from starting.
                                            The keys defined within a source may consist of any printable ASCII characters except '='.
                                            During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                          type: string
                                        optional:
                                          default: false
                                          description: |-
                                            Specify whether the file or its key must be defined. If the file or key
                                            does not exist, then the env var is not published.
                                            If optional is set to true and the specified key does not exist,
                                            the environment variable will not be set in the Pod's containers.

                                            If optional is set to false and the specified key does not exist,
                                            an error will be returned during Pod creation.
                                          type: boolean
                                        path:
                                          description: |-
                                            The path within the volume from which to select the file.
                                            Must be relative and may not contain the '..' path or start with '..'.
                                          type: string
                                        volumeName:
                                          description: The name of the volume mount
                                            containing the env file.
                                          type: string
                                      required:
                                      - key
                                      - path
                                      - volumeName
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    resourceFieldRef:
                                      description: |-
                                        Selects a resource of the container: only resources limits and requests
                                        (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                      properties:
                                        containerName:
                                          description: 'Container name: required for
                                            volumes, optional for env vars'
                                          type: string
                                        divisor:
                                          anyOf:
                                          - type: integer
                                          - type: string
                                          description: Specifies the output format
                                            of the exposed resources, defaults to
                                            "1"
                                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                          x-kubernetes-int-or-string: true
                                        resource:
                                          description: 'Required: resource to select'
                                          type: string
                                      required:
                                      - resource
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secretKeyRef:
                                      description: Selects a key of a secret in the
                                        pod's namespace
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the Secret
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                              required:
                              - name
                              type: object
                            type: array
                          resources:
                            description: Resources specifies the resource requirements
                              for the container.
                            properties:
                              claims:
                                description: |-
                                  Claims lists the names of resources, defined in spec.resourceClaims,
                                  that are used by this container.

                                  This field depends on the
                                  DynamicResourceAllocation feature gate.

                                  This field is immutable. It can only be set for containers.
                                items:
                                  description: ResourceClaim references one entry
                                    in PodSpec.ResourceClaims.
                                  properties:
                                    name:
                                      description: |-
                                        Name must match the name of one entry in pod.spec.resourceClaims of
                                        the Pod where this field is used. It makes that resource available
                                        inside a container.
                                      type: string
                                    request:
                                      description: |-
                                        Request is the name chosen for a request in the referenced claim.
                                        If empty, everything from the claim is made available, otherwise
                                        only the result of this request.
                                      type: string
                                  required:
                                  - name
                                  type: object
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                              limits:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Limits describes the maximum amount of compute resources allowed.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                              requests:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Requests describes the minimum amount of compute resources required.
                                  If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                  otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                            type: object
                        type: object
                      logEncoder:
                        description: |-
                          LogEncoder sets the log encoding format for the image-controller.
                          When not set, the upstream default (json) is used.
                        enum:
                        - json
                        - console
                        type: string
                      notificationResetter:
                        description: |-
                          NotificationResetter defines customizations for the notification-resetter CronJob
                          container (resources, env vars). When not set, the upstream defaults apply.
                        properties:
                          env:
                            description: Env specifies environment variables for the
                              container.
                            items:
                              description: EnvVar represents an environment variable
                                present in a Container.
                              properties:
                                name:
                                  description: |-
                                    Name of the environment variable.
                                    May consist of any printable ASCII characters except '='.
                                  type: string
                                value:
                                  description: |-
                                    Variable references $(VAR_NAME) are expanded
                                    using the previously defined environment variables in the container and
                                    any service environment variables. If a variable cannot be resolved,
                                    the reference in the input string will be unchanged. Double $$ are reduced
                                    to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                    "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                    Escaped references will never be expanded, regardless of whether the variable
                                    exists or not.
                                    Defaults to "".
                                  type: string
                                valueFrom:
                                  description: Source for the environment variable's
                                    value. Cannot be used if value is not empty.
                                  properties:
                                    configMapKeyRef:
                                      description: Selects a key of a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    fieldRef:
                                      description: |-
                                        Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                        spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                      properties:
                                        apiVersion:
                                          description: Version of the schema the FieldPath
                                            is written in terms of, defaults to "v1".
                                          type: string
                                        fieldPath:
                                          description: Path of the field to select
                                            in the specified API version.
                                          type: string
                                      required:
                                      - fieldPath
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    fileKeyRef:
                                      description: |-
                                        FileKeyRef selects a key of the env file.
                                        Requires the EnvFiles feature gate to be enabled.
                                      properties:
                                        key:
                                          description: |-
                                            The key within the env file. An invalid key will prevent the pod from starting.
                                            The keys defined within a source may consist of any printable ASCII characters except '='.
                                            During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                          type: string
                                        optional:
                                          default: false
                                          description: |-
                                            Specify whether the file or its key must be defined. If the file or key
                                            does not exist, then the env var is not published.
                                            If optional is set to true and the specified key does not exist,
                                            the environment variable will not be set in the Pod's containers.

                                            If optional is set to false and the specified key does not exist,
                                            an error will be returned during Pod creation.
                                          type: boolean
                                        path:
                                          description: |-
                                            The path within the volume from which to select the file.
                                            Must be relative and may not contain the '..' path or start with '..'.
                                          type: string
                                        volumeName:
                                          description: The name of the volume mount
                                            containing the env file.
                                          type: string
                                      required:
                                      - key
                                      - path
                                      - volumeName
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    resourceFieldRef:
                                      description: |-
                                        Selects a resource of the container: only resources limits and requests
                                        (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                      properties:
                                        containerName:
                                          description: 'Container name: required for
                                            volumes, optional for env vars'
                                          type: string
                                        divisor:
                                          anyOf:
                                          - type: integer
                                          - type: string
                                          description: Specifies the output format
                                            of the exposed resources, defaults to
                                            "1"
                                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                          x-kubernetes-int-or-string: true
                                        resource:
                                          description: 'Required: resource to select'
                                          type: string
                                      required:
                                      - resource
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secretKeyRef:
                                      description: Selects a key of a secret in the
                                        pod's namespace
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the Secret
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                              required:
                              - name
                              type: object
                            type: array
                          resources:
                            description: Resources specifies the resource requirements
                              for the container.
                            properties:
                              claims:
                                description: |-
                                  Claims lists the names of resources, defined in spec.resourceClaims,
                                  that are used by this container.

                                  This field depends on the
                                  DynamicResourceAllocation feature gate.

                                  This field is immutable. It can only be set for containers.
                                items:
                                  description: ResourceClaim references one entry
                                    in PodSpec.ResourceClaims.
                                  properties:
                                    name:
                                      description: |-
                                        Name must match the name of one entry in pod.spec.resourceClaims of
                                        the Pod where this field is used. It makes that resource available
                                        inside a container.
                                      type: string
                                    request:
                                      description: |-
                                        Request is the name chosen for a request in the referenced claim.
                                        If empty, everything from the claim is made available, otherwise
                                        only the result of this request.
                                      type: string
                                  required:
                                  - name
                                  type: object
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                              limits:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Limits describes the maximum amount of compute resources allowed.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                              requests:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Requests describes the minimum amount of compute resources required.
                                  If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                  otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                            type: object
                        type: object
                      quayCABundle:
                        description: |-
                          QuayCABundle configures a custom CA bundle for Quay registry communication.
                          When set, the CA certificate from the referenced ConfigMap is mounted into the
                          image-controller pod and used for TLS verification when connecting to Quay.
                          This is required when using a self-hosted Quay registry with a custom CA.
                        properties:
                          configMapName:
                            description: ConfigMapName is the name of the ConfigMap
                              containing the CA certificate.
                            minLength: 1
                            type: string
                          key:
                            description: |-
                              Key is the key within the ConfigMap that contains the CA certificate in PEM format.
                              Must be a plain filename without path separators or directory traversal sequences.
                            minLength: 1
                            pattern: ^[a-zA-Z0-9][a-zA-Z0-9._-]*$
                            type: string
                        required:
                        - configMapName
                        - key
                        type: object
                    type: object
                type: object
              info:
                description: |-
                  KonfluxInfo configures the info component.
                  The runtime configuration is copied to the KonfluxInfo CR by the operator.
                properties:
                  spec:
                    description: Spec configures the info component.
                    properties:
                      banner:
                        description: |-
                          Banner defines the configuration for the banner-content.yaml ConfigMap.
                          If not specified, an empty banner array will be used.
                        properties:
                          items:
                            description: Items is the list of banners to display
                            items:
                              description: BannerItem contains individual banner configuration
                              properties:
                                dayOfMonth:
                                  description: DayOfMonth is the day of month (1-31)
                                  maximum: 31
                                  minimum: 1
                                  type: integer
                                dayOfWeek:
                                  description: DayOfWeek is the day of week (0-6,
                                    0=Sunday)
                                  maximum: 6
                                  minimum: 0
                                  type: integer
                                endTime:
                                  description: EndTime is the end time in HH:mm format
                                    (required if date fields are set)
                                  type: string
                                month:
                                  description: Month is the month (1-12)
                                  maximum: 12
                                  minimum: 1
                                  type: integer
                                startTime:
                                  description: StartTime is the start time in HH:mm
                                    format (required if date fields are set)
                                  type: string
                                summary:
                                  description: Summary is the banner text (5-500 chars,
                                    supports Markdown)
                                  maxLength: 500
                                  minLength: 5
                                  type: string
                                timeZone:
                                  description: TimeZone is the IANA timezone (optional,
                                    defaults to UTC)
                                  type: string
                                type:
                                  description: Type is the banner type (info, warning,
                                    danger)
                                  enum:
                                  - info
                                  - warning
                                  - danger
                                  type: string
                                year:
                                  description: Year is the year for one-time banners
                                  type: integer
                              required:
                              - summary
                              - type
                              type: object
                            type: array
                        type: object
                      clusterConfig:
                        description: |-
                          ClusterConfig defines cluster-wide key-value configuration.
                          The key-value pairs will be stored in a ConfigMap named "cluster-config"
                          in the "konflux-info" namespace, readable by all authenticated users.
                          User-provided values take precedence over auto-detected values.
                        properties:
                          data:
                            description: |-
                              Data contains structured cluster-wide configuration values.
                              These values will be stored in the "cluster-config" ConfigMap in the "konflux-info" namespace.
                              The ConfigMap keys are stable and part of the public API consumed by PipelineRuns.
                              WARNING: Changing field names or JSON tags is a BREAKING CHANGE that will affect
                              all PipelineRuns reading from the ConfigMap. Field names must remain stable.
                            properties:
                              allowCacheProxy:
                                description: |-
                                  AllowCacheProxy enables the HTTP caching proxy for builds.
                                  When true, the "allow-cache-proxy" key is set to "true" in the cluster-config ConfigMap.
                                  When false, the key is set to "false". When nil (omitted), the key is absent.
                                type: boolean
                              allowPackageRegistryProxy:
                                description: |-
                                  AllowPackageRegistryProxy enables the package registry proxy for builds.
                                  When true, the "allow-package-registry-proxy" key is set to "true" in the cluster-config ConfigMap.
                                  When false, the key is set to "false". When nil (omitted), the key is absent.
                                type: boolean
                              buildIdentityRegexp:
                                description: |-
                                  BuildIdentityRegexp is a regex pattern used for matching the identity
                                  that signed artifacts as part of the build pipeline.
                                type: string
                              defaultOIDCIssuer:
                                description: DefaultOIDCIssuer is the default OIDC
                                  issuer URL.
                                type: string
                              enableKeylessSigning:
                                description: |-
                                  EnableKeylessSigning determines if pipelines should perform/validate keyless signing.
                                  When nil, the key is omitted from the ConfigMap (unset).
                                type: boolean
                              fulcioExternalUrl:
                                description: FulcioExternalUrl is the external Fulcio
                                  URL.
                                type: string
                              fulcioInternalUrl:
                                description: FulcioInternalUrl is the internal Fulcio
                                  URL.
                                type: string
                              httpProxy:
                                description: |-
                                  HTTPProxy is the HTTP proxy URL for builds (e.g., "squid.caching.svc.cluster.local:3128").
                                  Written as "http-proxy" in the cluster-config ConfigMap.
                                  Do not embed credentials in this URL — the ConfigMap is readable by all authenticated users.
                                type: string
                                x-kubernetes-validations:
                                - message: URL must not contain credentials (userinfo).
                                    Use a Secret for proxy authentication.
                                  rule: '!self.contains(''@'')'
                              noProxy:
                                description: |-
                                  NoProxy is a comma-separated list of hosts that should bypass the proxy.
                                  Written as "no-proxy" in the cluster-config ConfigMap.
                                  An empty string is a valid value meaning no hosts are excluded from proxying.
                                  When nil (omitted), the key is absent from the ConfigMap.
                                type: string
                              packageRegistryProxyNpmUrl:
                                description: |-
                                  PackageRegistryProxyNpmURL is the URL of the npm package registry proxy.
                                  Written as "package-registry-proxy-npm-url" in the cluster-config ConfigMap.
                                  Do not embed credentials in this URL — the ConfigMap is readable by all authenticated users.
                                type: string
                                x-kubernetes-validations:
                                - message: URL must not contain credentials (userinfo).
                                    Use a Secret for proxy authentication.
                                  rule: '!self.contains(''@'')'
                              packageRegistryProxyYarnUrl:
                                description: |-
                                  PackageRegistryProxyYarnURL is the URL of the yarn package registry proxy.
                                  Written as "package-registry-proxy-yarn-url" in the cluster-config ConfigMap.
                                  Do not embed credentials in this URL — the ConfigMap is readable by all authenticated users.
                                type: string
                                x-kubernetes-validations:
                                - message: URL must not contain credentials (userinfo).
                                    Use a Secret for proxy authentication.
                                  rule: '!self.contains(''@'')'
                              rekorExternalUrl:
                                description: RekorExternalUrl is the external Rekor
                                  URL.
                                type: string
                              rekorInternalUrl:
                                description: RekorInternalUrl is the internal Rekor
                                  URL.
                                type: string
                              tektonChainsIdentity:
                                description: |-
                                  TektonChainsIdentity is the identity of Tekton Chains used when verifying
                                  the signature of attestations produced by Tekton Chains.
                                type: string
                              trustifyOIDCIssuerUrl:
                                description: |-
                                  TrustifyOIDCIssuerUrl is the URL of the OIDC issuer
                                  used by Trustification clients.
                                type: string
                              trustifyServerExternalUrl:
                                description: TrustifyServerExternalUrl is the external
                                  URL for the Trustify server.
                                type: string
                              trustifyServerInternalUrl:
                                description: TrustifyServerInternalUrl is the internal
                                  URL for the Trustify server.
                                type: string
                              tufExternalUrl:
                                description: TufExternalUrl is the external TUF URL.
                                type: string
                              tufInternalUrl:
                                description: TufInternalUrl is the internal TUF URL.
                                type: string
                            type: object
                        type: object
                      publicInfo:
                        description: |-
                          PublicInfo defines the configuration for the info.json ConfigMap.
                          If not specified, default development values will be used.
                        properties:
                          environment:
                            default: development
                            description: Environment is the environment type (development,
                              production, staging)
                            enum:
                            - development
                            - production
                            - staging
                            type: string
                          integrations:
                            description: Integrations contains integration configuration
                            properties:
                              github:
                                description: GitHub contains GitHub integration configuration
                                properties:
                                  application_url:
                                    description: ApplicationURL is the GitHub App
                                      installation URL
                                    type: string
                                required:
                                - application_url
                                type: object
                              image_controller:
                                description: ImageController contains image controller
                                  configuration
                                properties:
                                  enabled:
                                    description: Enabled indicates if image controller
                                      is enabled
                                    type: boolean
                                  notifications:
                                    description: Notifications contains notification
                                      configurations
                                    items:
                                      description: InfoNotificationConfig contains
                                        notification configuration for info.json
                                      properties:
                                        config:
                                          description: |-
                                            Config contains method-specific configuration (as JSON).
                                            For webhook method, use: {"url": "https://webhook.example.com/endpoint"}
                                            For email method, use: {"email": "notifications@example.com"}
                                            Example webhook config:
                                              config:
                                                url: "https://webhook.example.com/build"
                                            Example email config:
                                              config:
                                                email: "team@example.com"
                                          x-kubernetes-preserve-unknown-fields: true
                                        event:
                                          description: Event is the event type (e.g.,
                                            "repo_push", "build_complete")
                                          type: string
                                        method:
                                          description: Method is the notification
                                            method (e.g., "webhook", "email")
                                          type: string
                                        title:
                                          description: Title is the notification title
                                          type: string
                                      required:
                                      - config
                                      - event
                                      - method
                                      - title
                                      type: object
                                    type: array
                                required:
                                - enabled
                                type: object
                              sbom_server:
                                description: SBOMServer contains SBOM server configuration
                                properties:
                                  sbom_sha:
                                    description: SBOMSha is the SBOM SHA URL
                                    type: string
                                  url:
                                    description: URL is the SBOM content URL
                                    type: string
                                required:
                                - sbom_sha
                                - url
                                type: object
                            type: object
                          rbac:
                            description: RBAC contains RBAC role definitions
                            items:
                              description: RBACRole contains RBAC role definition
                              properties:
                                description:
                                  description: Description is the role description
                                  type: string
                                displayName:
                                  description: |-
                                    DisplayName is the human-readable name displayed in the UI.
                                    If not specified, defaults to the Name field.
                                  type: string
                                name:
                                  description: Name is the ClusterRole name (e.g.,
                                    "konflux-admin-user-actions")
                                  type: string
                              required:
                              - description
                              - name
                              type: object
                            type: array
                          statusPageUrl:
                            description: StatusPageUrl is the URL to the status page
                            type: string
                          visibility:
                            default: public
                            description: Visibility is the visibility level (public,
                              private)
                            enum:
                            - public
                            - private
                            type: string
                        required:
                        - environment
                        - visibility
                        type: object
                    type: object
                type: object
              integrationService:
                description: |-
                  KonfluxIntegrationService configures the integration-service component.
                  The runtime configuration is copied to the KonfluxIntegrationService CR by the operator.
                properties:
                  spec:
                    description: Spec configures the integration-service component
                      (excludes componentMetrics; see spec.componentMetrics).
                    properties:
                      finallyTimeout:
                        description: |-
                          FinallyTimeout is the timeout for finally tasks (e.g. "2h", "30m").
                          Maps to the FINALLY_TIMEOUT env var on the controller-manager container.
                          Takes precedence over any FINALLY_TIMEOUT entry in integrationControllerManager.manager.env.
                          When omitted, the upstream integration-service default applies.
                        minLength: 2
                        pattern: ^([0-9]+h)?([0-9]+m)?([0-9]+s)?$
                        type: string
                      integrationControllerManager:
                        description: IntegrationControllerManager defines customizations
                          for the controller-manager deployment.
                        properties:
                          manager:
                            description: Manager defines customizations for the manager
                              container.
                            properties:
                              env:
                                description: Env specifies environment variables for
                                  the container.
                                items:
                                  description: EnvVar represents an environment variable
                                    present in a Container.
                                  properties:
                                    name:
                                      description: |-
                                        Name of the environment variable.
                                        May consist of any printable ASCII characters except '='.
                                      type: string
                                    value:
                                      description: |-
                                        Variable references $(VAR_NAME) are expanded
                                        using the previously defined environment variables in the container and
                                        any service environment variables. If a variable cannot be resolved,
                                        the reference in the input string will be unchanged. Double $$ are reduced
                                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                        Escaped references will never be expanded, regardless of whether the variable
                                        exists or not.
                                        Defaults to "".
                                      type: string
                                    valueFrom:
                                      description: Source for the environment variable's
                                        value. Cannot be used if value is not empty.
                                      properties:
                                        configMapKeyRef:
                                          description: Selects a key of a ConfigMap.
                                          properties:
                                            key:
                                              description: The key to select.
                                              type: string
                                            name:
                                              default: ""
                                              description: |-
                                                Name of the referent.
                                                This field is effectively required, but due to backwards compatibility is
                                                allowed to be empty. Instances of this type with an empty value here are
                                                almost certainly wrong.
                                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                              type: string
                                            optional:
                                              description: Specify whether the ConfigMap
                                                or its key must be defined
                                              type: boolean
                                          required:
                                          - key
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        fieldRef:
                                          description: |-
                                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                          properties:
                                            apiVersion:
                                              description: Version of the schema the
                                                FieldPath is written in terms of,
                                                defaults to "v1".
                                              type: string
                                            fieldPath:
                                              description: Path of the field to select
                                                in the specified API version.
                                              type: string
                                          required:
                                          - fieldPath
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        fileKeyRef:
                                          description: |-
                                            FileKeyRef selects a key of the env file.
                                            Requires the EnvFiles feature gate to be enabled.
                                          properties:
                                            key:
                                              description: |-
                                                The key within the env file. An invalid key will prevent the pod from starting.
                                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                              type: string
                                            optional:
                                              default: false
                                              description: |-
                                                Specify whether the file or its key must be defined. If the file or key
                                                does not exist, then the env var is not published.
                                                If optional is set to true and the specified key does not exist,
                                                the environment variable will not be set in the Pod's containers.

                                                If optional is set to false and the specified key does not exist,
                                                an error will be returned during Pod creation.
                                              type: boolean
                                            path:
                                              description: |-
                                                The path within the volume from which to select the file.
                                                Must be relative and may not contain the '..' path or start with '..'.
                                              type: string
                                            volumeName:
                                              description: The name of the volume
                                                mount containing the env file.
                                              type: string
                                          required:
                                          - key
                                          - path
                                          - volumeName
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        resourceFieldRef:
                                          description: |-
                                            Selects a resource of the container: only resources limits and requests
                                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                          properties:
                                            containerName:
                                              description: 'Container name: required
                                                for volumes, optional for env vars'
                                              type: string
                                            divisor:
                                              anyOf:
                                              - type: integer
                                              - type: string
                                              description: Specifies the output format
                                                of the exposed resources, defaults
                                                to "1"
                                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                              x-kubernetes-int-or-string: true
                                            resource:
                                              description: 'Required: resource to
                                                select'
                                              type: string
                                          required:
                                          - resource
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        secretKeyRef:
                                          description: Selects a key of a secret in
                                            the pod's namespace
                                          properties:
                                            key:
                                              description: The key of the secret to
                                                select from.  Must be a valid secret
                                                key.
                                              type: string
                                            name:
                                              default: ""
                                              description: |-
                                                Name of the referent.
                                                This field is effectively required, but due to backwards compatibility is
                                                allowed to be empty. Instances of this type with an empty value here are
                                                almost certainly wrong.
                                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                              type: string
                                            optional:
                                              description: Specify whether the Secret
                                                or its key must be defined
                                              type: boolean
                                          required:
                                          - key
                                          type: object
                                          x-kubernetes-map-type: atomic
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              resources:
                                description: Resources specifies the resource requirements
                                  for the container.
                                properties:
                                  claims:
                                    description: |-
                                      Claims lists the names of resources, defined in spec.resourceClaims,
                                      that are used by this container.

                                      This field depends on the
                                      DynamicResourceAllocation feature gate.

                                      This field is immutable. It can only be set for containers.
                                    items:
                                      description: ResourceClaim references one entry
                                        in PodSpec.ResourceClaims.
                                      properties:
                                        name:
                                          description: |-
                                            Name must match the name of one entry in pod.spec.resourceClaims of
                                            the Pod where this field is used. It makes that resource available
                                            inside a container.
                                          type: string
                                        request:
                                          description: |-
                                            Request is the name chosen for a request in the referenced claim.
                                            If empty, everything from the claim is made available, otherwise
                                            only the result of this request.
                                          type: string
                                      required:
                                      - name
                                      type: object
                                    type: array
                                    x-kubernetes-list-map-keys:
                                    - name
                                    x-kubernetes-list-type: map
                                  limits:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: |-
                                      Limits describes the maximum amount of compute resources allowed.
                                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    type: object
                                  requests:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: |-
                                      Requests describes the minimum amount of compute resources required.
                                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    type: object
                                type: object
                            type: object
                          replicas:
                            default: 1
                            description: Replicas is the number of replicas for the
                              controller-manager deployment.
                            format: int32
                            minimum: 1
                            type: integer
                        type: object
                      minSnapshotsToKeepPerComponent:
                        description: |-
                          MinSnapshotsToKeepPerComponent is the minimum number of snapshots to retain per component,
                          regardless of PR/non-PR classification. Injected as the MIN_SNAPSHOTS_TO_KEEP_PER_COMPONENT
                          env var on the GC container.
                          Takes precedence over any MIN_SNAPSHOTS_TO_KEEP_PER_COMPONENT entry in snapshotGarbageCollector.env.
                          When omitted, the upstream integration-service default applies.
                        pattern: ^[0-9]+$
                        type: string
                      nonPRSnapshotsToKeep:
                        description: |-
                          NonPRSnapshotsToKeep is the number of snapshots to retain per component for non-PR
                          pipeline runs. Maps to the NON_PR_SNAPSHOTS_TO_KEEP env var on the GC container.
                          Takes precedence over any NON_PR_SNAPSHOTS_TO_KEEP entry in snapshotGarbageCollector.env.
                          When omitted, the upstream integration-service default applies.
                        pattern: ^[0-9]+$
                        type: string
                      pipelineTimeout:
                        description: |-
                          PipelineTimeout is the overall pipeline run timeout (e.g. "6h", "1h30m", "90m").
                          Maps to the PIPELINE_TIMEOUT env var on the controller-manager container.
                          Takes precedence over any PIPELINE_TIMEOUT entry in integrationControllerManager.manager.env.
                          When omitted, the upstream integration-service default applies.
                        minLength: 2
                        pattern: ^([0-9]+h)?([0-9]+m)?([0-9]+s)?$
                        type: string
                      prSnapshotsToKeep:
                        description: |-
                          PRSnapshotsToKeep is the number of snapshots to retain per component for PR-triggered
                          pipeline runs. Maps to the PR_SNAPSHOTS_TO_KEEP env var on the GC container.
                          Takes precedence over any PR_SNAPSHOTS_TO_KEEP entry in snapshotGarbageCollector.env.
                          When omitted, the upstream integration-service default applies.
                        pattern: ^[0-9]+$
                        type: string
                      snapshotGarbageCollector:
                        description: |-
                          SnapshotGarbageCollector defines customizations for the snapshot GC CronJob container
                          (resources, env vars).
                        properties:
                          env:
                            description: Env specifies environment variables for the
                              container.
                            items:
                              description: EnvVar represents an environment variable
                                present in a Container.
                              properties:
                                name:
                                  description: |-
                                    Name of the environment variable.
                                    May consist of any printable ASCII characters except '='.
                                  type: string
                                value:
                                  description: |-
                                    Variable references $(VAR_NAME) are expanded
                                    using the previously defined environment variables in the container and
                                    any service environment variables. If a variable cannot be resolved,
                                    the reference in the input string will be unchanged. Double $$ are reduced
                                    to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                    "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                    Escaped references will never be expanded, regardless of whether the variable
                                    exists or not.
                                    Defaults to "".
                                  type: string
                                valueFrom:
                                  description: Source for the environment variable's
                                    value. Cannot be used if value is not empty.
                                  properties:
                                    configMapKeyRef:
                                      description: Selects a key of a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    fieldRef:
                                      description: |-
                                        Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                        spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                      properties:
                                        apiVersion:
                                          description: Version of the schema the FieldPath
                                            is written in terms of, defaults to "v1".
                                          type: string
                                        fieldPath:
                                          description: Path of the field to select
                                            in the specified API version.
                                          type: string
                                      required:
                                      - fieldPath
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    fileKeyRef:
                                      description: |-
                                        FileKeyRef selects a key of the env file.
                                        Requires the EnvFiles feature gate to be enabled.
                                      properties:
                                        key:
                                          description: |-
                                            The key within the env file. An invalid key will prevent the pod from starting.
                                            The keys defined within a source may consist of any printable ASCII characters except '='.
                                            During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                          type: string
                                        optional:
                                          default: false
                                          description: |-
                                            Specify whether the file or its key must be defined. If the file or key
                                            does not exist, then the env var is not published.
                                            If optional is set to true and the specified key does not exist,
                                            the environment variable will not be set in the Pod's containers.

                                            If optional is set to false and the specified key does not exist,
                                            an error will be returned during Pod creation.
                                          type: boolean
                                        path:
                                          description: |-
                                            The path within the volume from which to select the file.
                                            Must be relative and may not contain the '..' path or start with '..'.
                                          type: string
                                        volumeName:
                                          description: The name of the volume mount
                                            containing the env file.
                                          type: string
                                      required:
                                      - key
                                      - path
                                      - volumeName
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    resourceFieldRef:
                                      description: |-
                                        Selects a resource of the container: only resources limits and requests
                                        (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                      properties:
                                        containerName:
                                          description: 'Container name: required for
                                            volumes, optional for env vars'
                                          type: string
                                        divisor:
                                          anyOf:
                                          - type: integer
                                          - type: string
                                          description: Specifies the output format
                                            of the exposed resources, defaults to
                                            "1"
                                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                          x-kubernetes-int-or-string: true
                                        resource:
                                          description: 'Required: resource to select'
                                          type: string
                                      required:
                                      - resource
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secretKeyRef:
                                      description: Selects a key of a secret in the
                                        pod's namespace
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the Secret
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                              required:
                              - name
                              type: object
                            type: array
                          resources:
                            description: Resources specifies the resource requirements
                              for the container.
                            properties:
                              claims:
                                description: |-
                                  Claims lists the names of resources, defined in spec.resourceClaims,
                                  that are used by this container.

                                  This field depends on the
                                  DynamicResourceAllocation feature gate.

                                  This field is immutable. It can only be set for containers.
                                items:
                                  description: ResourceClaim references one entry
                                    in PodSpec.ResourceClaims.
                                  properties:
                                    name:
                                      description: |-
                                        Name must match the name of one entry in pod.spec.resourceClaims of
                                        the Pod where this field is used. It makes that resource available
                                        inside a container.
                                      type: string
                                    request:
                                      description: |-
                                        Request is the name chosen for a request in the referenced claim.
                                        If empty, everything from the claim is made available, otherwise
                                        only the result of this request.
                                      type: string
                                  required:
                                  - name
                                  type: object
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                              limits:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Limits describes the maximum amount of compute resources allowed.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                              requests:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Requests describes the minimum amount of compute resources required.
                                  If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                  otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                            type: object
                        type: object
                      tasksTimeout:
                        description: |-
                          TasksTimeout is the timeout for tasks within a pipeline run (e.g. "4h", "90m").
                          Maps to the TASKS_TIMEOUT env var on the controller-manager container.
                          Takes precedence over any TASKS_TIMEOUT entry in integrationControllerManager.manager.env.
                          When omitted, the upstream integration-service default applies.
                        minLength: 2
                        pattern: ^([0-9]+h)?([0-9]+m)?([0-9]+s)?$
                        type: string
                    type: object
                type: object
              internalRegistry:
                description: |-
                  InternalRegistry configures the internal registry component.
                  The runtime configuration is copied to the KonfluxInternalRegistry CR by the operator.
                  Enabling internal registry requires trust-manager to be deployed.
                properties:
                  enabled:
                    description: |-
                      Enabled controls whether internal registry resources are deployed.
                      Defaults to false if not specified.
                    type: boolean
                type: object
              namespaceLister:
                description: |-
                  NamespaceLister configures the namespace-lister component.
                  The runtime configuration is copied to the KonfluxNamespaceLister CR by the operator.
                properties:
                  spec:
                    description: Spec configures the namespace-lister component.
                    properties:
                      cacheResyncPeriod:
                        description: |-
                          CacheResyncPeriod controls how often the namespace-lister's access cache
                          fully re-evaluates RBAC permissions as a safety net.
                          Accepts a Go duration string (e.g. "5s", "10m", "1h").
                          When omitted, the namespace-lister relies solely on watch events
                          with no periodic resync.
                        pattern: ^([0-9]+(s|m|h))+$
                        type: string
                      logLevel:
                        description: |-
                          LogLevel sets the minimum log severity for the namespace-lister.
                          Accepted values: debug, info, warn, error.
                          When omitted, the namespace-lister defaults to error level.
                        enum:
                        - debug
                        - info
                        - warn
                        - error
                        type: string
                      namespaceLister:
                        description: NamespaceLister defines customizations for the
                          namespace-lister deployment.
                        properties:
                          namespaceLister:
                            description: NamespaceLister defines customizations for
                              the namespace-lister container.
                            properties:
                              env:
                                description: Env specifies environment variables for
                                  the container.
                                items:
                                  description: EnvVar represents an environment variable
                                    present in a Container.
                                  properties:
                                    name:
                                      description: |-
                                        Name of the environment variable.
                                        May consist of any printable ASCII characters except '='.
                                      type: string
                                    value:
                                      description: |-
                                        Variable references $(VAR_NAME) are expanded
                                        using the previously defined environment variables in the container and
                                        any service environment variables. If a variable cannot be resolved,
                                        the reference in the input string will be unchanged. Double $$ are reduced
                                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                        Escaped references will never be expanded, regardless of whether the variable
                                        exists or not.
                                        Defaults to "".
                                      type: string
                                    valueFrom:
                                      description: Source for the environment variable's
                                        value. Cannot be used if value is not empty.
                                      properties:
                                        configMapKeyRef:
                                          description: Selects a key of a ConfigMap.
                                          properties:
                                            key:
                                              description: The key to select.
                                              type: string
                                            name:
                                              default: ""
                                              description: |-
                                                Name of the referent.
                                                This field is effectively required, but due to backwards compatibility is
                                                allowed to be empty. Instances of this type with an empty value here are
                                                almost certainly wrong.
                                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                              type: string
                                            optional:
                                              description: Specify whether the ConfigMap
                                                or its key must be defined
                                              type: boolean
                                          required:
                                          - key
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        fieldRef:
                                          description: |-
                                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                          properties:
                                            apiVersion:
                                              description: Version of the schema the
                                                FieldPath is written in terms of,
                                                defaults to "v1".
                                              type: string
                                            fieldPath:
                                              description: Path of the field to select
                                                in the specified API version.
                                              type: string
                                          required:
                                          - fieldPath
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        fileKeyRef:
                                          description: |-
                                            FileKeyRef selects a key of the env file.
                                            Requires the EnvFiles feature gate to be enabled.
                                          properties:
                                            key:
                                              description: |-
                                                The key within the env file. An invalid key will prevent the pod from starting.
                                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                              type: string
                                            optional:
                                              default: false
                                              description: |-
                                                Specify whether the file or its key must be defined. If the file or key
                                                does not exist, then the env var is not published.
                                                If optional is set to true and the specified key does not exist,
                                                the environment variable will not be set in the Pod's containers.

                                                If optional is set to false and the specified key does not exist,
                                                an error will be returned during Pod creation.
                                              type: boolean
                                            path:
                                              description: |-
                                                The path within the volume from which to select the file.
                                                Must be relative and may not contain the '..' path or start with '..'.
                                              type: string
                                            volumeName:
                                              description: The name of the volume
                                                mount containing the env file.
                                              type: string
                                          required:
                                          - key
                                          - path
                                          - volumeName
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        resourceFieldRef:
                                          description: |-
                                            Selects a resource of the container: only resources limits and requests
                                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                          properties:
                                            containerName:
                                              description: 'Container name: required
                                                for volumes, optional for env vars'
                                              type: string
                                            divisor:
                                              anyOf:
                                              - type: integer
                                              - type: string
                                              description: Specifies the output format
                                                of the exposed resources, defaults
                                                to "1"
                                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                              x-kubernetes-int-or-string: true
                                            resource:
                                              description: 'Required: resource to
                                                select'
                                              type: string
                                          required:
                                          - resource
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        secretKeyRef:
                                          description: Selects a key of a secret in
                                            the pod's namespace
                                          properties:
                                            key:
                                              description: The key of the secret to
                                                select from.  Must be a valid secret
                                                key.
                                              type: string
                                            name:
                                              default: ""
                                              description: |-
                                                Name of the referent.
                                                This field is effectively required, but due to backwards compatibility is
                                                allowed to be empty. Instances of this type with an empty value here are
                                                almost certainly wrong.
                                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                              type: string
                                            optional:
                                              description: Specify whether the Secret
                                                or its key must be defined
                                              type: boolean
                                          required:
                                          - key
                                          type: object
                                          x-kubernetes-map-type: atomic
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              resources:
                                description: Resources specifies the resource requirements
                                  for the container.
                                properties:
                                  claims:
                                    description: |-
                                      Claims lists the names of resources, defined in spec.resourceClaims,
                                      that are used by this container.

                                      This field depends on the
                                      DynamicResourceAllocation feature gate.

                                      This field is immutable. It can only be set for containers.
                                    items:
                                      description: ResourceClaim references one entry
                                        in PodSpec.ResourceClaims.
                                      properties:
                                        name:
                                          description: |-
                                            Name must match the name of one entry in pod.spec.resourceClaims of
                                            the Pod where this field is used. It makes that resource available
                                            inside a container.
                                          type: string
                                        request:
                                          description: |-
                                            Request is the name chosen for a request in the referenced claim.
                                            If empty, everything from the claim is made available, otherwise
                                            only the result of this request.
                                          type: string
                                      required:
                                      - name
                                      type: object
                                    type: array
                                    x-kubernetes-list-map-keys:
                                    - name
                                    x-kubernetes-list-type: map
                                  limits:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: |-
                                      Limits describes the maximum amount of compute resources allowed.
                                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    type: object
                                  requests:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: |-
                                      Requests describes the minimum amount of compute resources required.
                                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    type: object
                                type: object
                            type: object
                          replicas:
                            default: 1
                            description: Replicas is the number of replicas for the
                              namespace-lister deployment.
                            format: int32
                            minimum: 1
                            type: integer
                        type: object
                    type: object
                type: object
              releaseService:
                description: |-
                  KonfluxReleaseService configures the release-service component.
                  The runtime configuration is copied to the KonfluxReleaseService CR by the operator.
                properties:
                  spec:
                    description: Spec configures the release-service component (excludes
                      componentMetrics; see spec.componentMetrics).
                    properties:
                      debug:
                        description: |-
                          Debug enables debug mode in the ReleaseServiceConfig.
                          When true, the release-service operates in debug mode.
                        type: boolean
                      emptyDirOverrides:
                        description: |-
                          EmptyDirOverrides defines pipeline patterns that should use emptyDir volumes
                          instead of PVCs. Applied to the ReleaseServiceConfig CR managed by the operator.
                        items:
                          description: EmptyDirOverride defines a pipeline pattern
                            that should use emptyDir volumes.
                          properties:
                            pathInRepo:
                              description: PathInRepo is the path to the pipeline
                                file within the repository.
                              type: string
                            revision:
                              description: Revision is a regex pattern matching the
                                pipeline revision.
                              type: string
                            url:
                              description: URL is a regex pattern matching the pipeline
                                repository URL.
                              type: string
                          required:
                          - pathInRepo
                          - revision
                          - url
                          type: object
                        type: array
                      releaseControllerManager:
                        description: ReleaseControllerManager defines customizations
                          for the controller-manager deployment.
                        properties:
                          manager:
                            description: Manager defines customizations for the manager
                              container.
                            properties:
                              env:
                                description: Env specifies environment variables for
                                  the container.
                                items:
                                  description: EnvVar represents an environment variable
                                    present in a Container.
                                  properties:
                                    name:
                                      description: |-
                                        Name of the environment variable.
                                        May consist of any printable ASCII characters except '='.
                                      type: string
                                    value:
                                      description: |-
                                        Variable references $(VAR_NAME) are expanded
                                        using the previously defined environment variables in the container and
                                        any service environment variables. If a variable cannot be resolved,
                                        the reference in the input string will be unchanged. Double $$ are reduced
                                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                        Escaped references will never be expanded, regardless of whether the variable
                                        exists or not.
                                        Defaults to "".
                                      type: string
                                    valueFrom:
                                      description: Source for the environment variable's
                                        value. Cannot be used if value is not empty.
                                      properties:
                                        configMapKeyRef:
                                          description: Selects a key of a ConfigMap.
                                          properties:
                                            key:
                                              description: The key to select.
                                              type: string
                                            name:
                                              default: ""
                                              description: |-
                                                Name of the referent.
                                                This field is effectively required, but due to backwards compatibility is
                                                allowed to be empty. Instances of this type with an empty value here are
                                                almost certainly wrong.
                                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                              type: string
                                            optional:
                                              description: Specify whether the ConfigMap
                                                or its key must be defined
                                              type: boolean
                                          required:
                                          - key
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        fieldRef:
                                          description: |-
                                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                          properties:
                                            apiVersion:
                                              description: Version of the schema the
                                                FieldPath is written in terms of,
                                                defaults to "v1".
                                              type: string
                                            fieldPath:
                                              description: Path of the field to select
                                                in the specified API version.
                                              type: string
                                          required:
                                          - fieldPath
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        fileKeyRef:
                                          description: |-
                                            FileKeyRef selects a key of the env file.
                                            Requires the EnvFiles feature gate to be enabled.
                                          properties:
                                            key:
                                              description: |-
                                                The key within the env file. An invalid key will prevent the pod from starting.
                                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                              type: string
                                            optional:
                                              default: false
                                              description: |-
                                                Specify whether the file or its key must be defined. If the file or key
                                                does not exist, then the env var is not published.
                                                If optional is set to true and the specified key does not exist,
                                                the environment variable will not be set in the Pod's containers.

                                                If optional is set to false and the specified key does not exist,
                                                an error will be returned during Pod creation.
                                              type: boolean
                                            path:
                                              description: |-
                                                The path within the volume from which to select the file.
                                                Must be relative and may not contain the '..' path or start with '..'.
                                              type: string
                                            volumeName:
                                              description: The name of the volume
                                                mount containing the env file.
                                              type: string
                                          required:
                                          - key
                                          - path
                                          - volumeName
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        resourceFieldRef:
                                          description: |-
                                            Selects a resource of the container: only resources limits and requests
                                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                          properties:
                                            containerName:
                                              description: 'Container name: required
                                                for volumes, optional for env vars'
                                              type: string
                                            divisor:
                                              anyOf:
                                              - type: integer
                                              - type: string
                                              description: Specifies the output format
                                                of the exposed resources, defaults
                                                to "1"
                                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                              x-kubernetes-int-or-string: true
                                            resource:
                                              description: 'Required: resource to
                                                select'
                                              type: string
                                          required:
                                          - resource
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        secretKeyRef:
                                          description: Selects a key of a secret in
                                            the pod's namespace
                                          properties:
                                            key:
                                              description: The key of the secret to
                                                select from.  Must be a valid secret
                                                key.
                                              type: string
                                            name:
                                              default: ""
                                              description: |-
                                                Name of the referent.
                                                This field is effectively required, but due to backwards compatibility is
                                                allowed to be empty. Instances of this type with an empty value here are
                                                almost certainly wrong.
                                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                              type: string
                                            optional:
                                              description: Specify whether the Secret
                                                or its key must be defined
                                              type: boolean
                                          required:
                                          - key
                                          type: object
                                          x-kubernetes-map-type: atomic
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              resources:
                                description: Resources specifies the resource requirements
                                  for the container.
                                properties:
                                  claims:
                                    description: |-
                                      Claims lists the names of resources, defined in spec.resourceClaims,
                                      that are used by this container.

                                      This field depends on the
                                      DynamicResourceAllocation feature gate.

                                      This field is immutable. It can only be set for containers.
                                    items:
                                      description: ResourceClaim references one entry
                                        in PodSpec.ResourceClaims.
                                      properties:
                                        name:
                                          description: |-
                                            Name must match the name of one entry in pod.spec.resourceClaims of
                                            the Pod where this field is used. It makes that resource available
                                            inside a container.
                                          type: string
                                        request:
                                          description: |-
                                            Request is the name chosen for a request in the referenced claim.
                                            If empty, everything from the claim is made available, otherwise
                                            only the result of this request.
                                          type: string
                                      required:
                                      - name
                                      type: object
                                    type: array
                                    x-kubernetes-list-map-keys:
                                    - name
                                    x-kubernetes-list-type: map
                                  limits:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: |-
                                      Limits describes the maximum amount of compute resources allowed.
                                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    type: object
                                  requests:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: |-
                                      Requests describes the minimum amount of compute resources required.
                                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    type: object
                                type: object
                            type: object
                          replicas:
                            default: 1
                            description: Replicas is the number of replicas for the
                              controller-manager deployment.
                            format: int32
                            minimum: 1
                            type: integer
                        type: object
                    type: object
                type: object
              telemetry:
                description: |-
                  Telemetry configures the user-facing telemetry component
                  (internally backed by segment-bridge).
                  When enabled, the operator deploys a CronJob that collects anonymized usage
                  data from the cluster and sends it to Segment for analysis.
                  The runtime configuration is copied to the KonfluxSegmentBridge CR by the operator.
                properties:
                  enabled:
                    description: |-
                      Enabled controls whether the telemetry CronJob is deployed.
                      Defaults to false if not specified.
                    type: boolean
                  spec:
                    description: Spec configures the telemetry component.
                    properties:
                      segmentAPIURL:
                        description: |-
                          SegmentAPIURL is the base URL of the Segment API endpoint, without "/batch".
                          The operator appends "/batch" to produce the SEGMENT_BATCH_API env var.
                          Example: "https://console.redhat.com/connections/api/v1"
                          When not specified, defaults to "https://api.segment.io/v1".
                          Only plain HTTPS base URLs are supported (no query strings or fragments).
                        pattern: ^https://[^?#]+$
                        type: string
                      segmentKey:
                        description: |-
                          SegmentKey is the write key used to authenticate with the Segment API.
                          When not specified, a default key baked into the operator build is used,
                          routing telemetry data to the Konflux dev team's Segment project.
                        type: string
                      tektonLimit:
                        description: |-
                          TektonLimit sets the maximum number of PipelineRun records fetched per CronJob run.
                          Defaults to 1000 (covers ~4.7h at avg production throughput of ~214 PipelineRuns/hour).
                        minimum: 1
                        type: integer
                    type: object
                type: object
              ui:
                description: |-
                  KonfluxUI configures the UI component.
                  The runtime configuration is copied to the KonfluxUI CR by the operator.
                properties:
                  spec:
                    description: Spec configures the UI component (excludes componentMetrics;
                      see spec.componentMetrics).
                    properties:
                      dex:
                        description: Dex defines customizations for the dex deployment.
                        properties:
                          config:
                            description: Config defines the Dex IdP configuration
                              parameters.
                            properties:
                              configureLoginWithOpenShift:
                                description: |-
                                  ConfigureLoginWithOpenShift enables the OpenShift connector for authentication.
                                  When true (or nil on OpenShift), an OpenShift connector is automatically added using the Kubernetes API.
                                  Set to false to explicitly disable OpenShift login even when running on OpenShift.
                                nullable: true
                                type: boolean
                              connectors:
                                description: Connectors are upstream identity provider
                                  connectors.
                                items:
                                  description: Connector represents an upstream identity
                                    provider connector.
                                  properties:
                                    config:
                                      description: Config contains connector-specific
                                        configuration.
                                      properties:
                                        bindDN:
                                          type: string
                                        bindPW:
                                          type: string
                                        clientID:
                                          description: Common OIDC/OAuth fields
                                          type: string
                                        clientSecret:
                                          type: string
                                        groupSearch:
                                          description: LDAPGroupSearch configures
                                            LDAP group search settings.
                                          properties:
                                            baseDN:
                                              type: string
                                            filter:
                                              type: string
                                            nameAttr:
                                              type: string
                                            userMatchers:
                                              items:
                                                description: LDAPUserMatcher configures
                                                  how users are matched to groups.
                                                properties:
                                                  groupAttr:
                                                    type: string
                                                  userAttr:
                                                    type: string
                                                type: object
                                              type: array
                                          type: object
                                        groups:
                                          items:
                                            type: string
                                          type: array
                                        host:
                                          description: LDAP-specific fields
                                          type: string
                                        insecureCA:
                                          type: boolean
                                        insecureNoSSL:
                                          type: boolean
                                        insecureSkipVerify:
                                          type: boolean
                                        issuer:
                                          type: string
                                        orgs:
                                          description: GitHub-specific fields
                                          items:
                                            description: GitHubOrg represents a GitHub
                                              organization for authentication.
                                            properties:
                                              name:
                                                type: string
                                              teams:
                                                items:
                                                  type: string
                                                type: array
                                            type: object
                                          type: array
                                        redirectURI:
                                          type: string
                                        rootCA:
                                          description: |-
                                            RootCA is the path to a trusted root certificate for verifying TLS connections.
                                            Used by connectors that need to verify the TLS certificate of the upstream provider.
                                          type: string
                                        userSearch:
                                          description: LDAPUserSearch configures LDAP
                                            user search settings.
                                          properties:
                                            baseDN:
                                              type: string
                                            emailAttr:
                                              type: string
                                            filter:
                                              type: string
                                            idAttr:
                                              type: string
                                            nameAttr:
                                              type: string
                                            username:
                                              type: string
                                          type: object
                                      type: object
                                    id:
                                      description: ID is a unique identifier for this
                                        connector.
                                      type: string
                                    name:
                                      description: Name is a human-readable name for
                                        this connector.
                                      type: string
                                    type:
                                      description: Type specifies the connector type
                                        (e.g., "oidc", "ldap", "github", "openshift").
                                      type: string
                                  type: object
                                type: array
                              enablePasswordDB:
                                description: |-
                                  EnablePasswordDB enables the local password database.
                                  When nil (not set), defaults to true if no connectors are configured.
                                nullable: true
                                type: boolean
                              hostname:
                                description: |-
                                  Hostname is the external hostname for the Dex issuer (e.g., "dex.example.com").
                                  If empty, the hostname is determined from the ingress configuration.
                                type: string
                              passwordConnector:
                                description: PasswordConnector specifies the connector
                                  ID to use for password grants (e.g., "local").
                                type: string
                              port:
                                description: |-
                                  Port is the external port for the Dex issuer (e.g., "9443").
                                  If empty, the port is determined from the ingress configuration (typically empty for HTTPS on 443).
                                type: string
                              staticPasswords:
                                description: StaticPasswords are predefined user credentials
                                  for the local password database.
                                items:
                                  description: Password represents a static user password
                                    entry.
                                  properties:
                                    email:
                                      description: Email is the user's email address
                                        (used as the login identifier).
                                      type: string
                                    groups:
                                      description: |-
                                        Groups is a list of groups the user belongs to.
                                        Requires Dex v2.45.0+. Groups are included in the ID token
                                        when the "groups" scope is requested.
                                      items:
                                        type: string
                                      type: array
                                    hash:
                                      description: Hash is the bcrypt hash of the
                                        user's password.
                                      type: string
                                    userID:
                                      description: UserID is a unique identifier for
                                        the user.
                                      type: string
                                    username:
                                      description: Username is the display name for
                                        the user.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          dex:
                            description: Dex defines customizations for the dex container.
                            properties:
                              env:
                                description: Env specifies environment variables for
                                  the container.
                                items:
                                  description: EnvVar represents an environment variable
                                    present in a Container.
                                  properties:
                                    name:
                                      description: |-
                                        Name of the environment variable.
                                        May consist of any printable ASCII characters except '='.
                                      type: string
                                    value:
                                      description: |-
                                        Variable references $(VAR_NAME) are expanded
                                        using the previously defined environment variables in the container and
                                        any service environment variables. If a variable cannot be resolved,
                                        the reference in the input string will be unchanged. Double $$ are reduced
                                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                        Escaped references will never be expanded, regardless of whether the variable
                                        exists or not.
                                        Defaults to "".
                                      type: string
                                    valueFrom:
                                      description: Source for the environment variable's
                                        value. Cannot be used if value is not empty.
                                      properties:
                                        configMapKeyRef:
                                          description: Selects a key of a ConfigMap.
                                          properties:
                                            key:
                                              description: The key to select.
                                              type: string
                                            name:
                                              default: ""
                                              description: |-
                                                Name of the referent.
                                                This field is effectively required, but due to backwards compatibility is
                                                allowed to be empty. Instances of this type with an empty value here are
                                                almost certainly wrong.
                                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                              type: string
                                            optional:
                                              description: Specify whether the ConfigMap
                                                or its key must be defined
                                              type: boolean
                                          required:
                                          - key
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        fieldRef:
                                          description: |-
                                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                          properties:
                                            apiVersion:
                                              description: Version of the schema the
                                                FieldPath is written in terms of,
                                                defaults to "v1".
                                              type: string
                                            fieldPath:
                                              description: Path of the field to select
                                                in the specified API version.
                                              type: string
                                          required:
                                          - fieldPath
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        fileKeyRef:
                                          description: |-
                                            FileKeyRef selects a key of the env file.
                                            Requires the EnvFiles feature gate to be enabled.
                                          properties:
                                            key:
                                              description: |-
                                                The key within the env file. An invalid key will prevent the pod from starting.
                                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                              type: string
                                            optional:
                                              default: false
                                              description: |-
                                                Specify whether the file or its key must be defined. If the file or key
                                                does not exist, then the env var is not published.
                                                If optional is set to true and the specified key does not exist,
                                                the environment variable will not be set in the Pod's containers.

                                                If optional is set to false and the specified key does not exist,
                                                an error will be returned during Pod creation.
                                              type: boolean
                                            path:
                                              description: |-
                                                The path within the volume from which to select the file.
                                                Must be relative and may not contain the '..' path or start with '..'.
                                              type: string
                                            volumeName:
                                              description: The name of the volume
                                                mount containing the env file.
                                              type: string
                                          required:
                                          - key
                                          - path
                                          - volumeName
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        resourceFieldRef:
                                          description: |-
                                            Selects a resource of the container: only resources limits and requests
                                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                          properties:
                                            containerName:
                                              description: 'Container name: required
                                                for volumes, optional for env vars'
                                              type: string
                                            divisor:
                                              anyOf:
                                              - type: integer
                                              - type: string
                                              description: Specifies the output format
                                                of the exposed resources, defaults
                                                to "1"
                                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                              x-kubernetes-int-or-string: true
                                            resource:
                                              description: 'Required: resource to
                                                select'
                                              type: string
                                          required:
                                          - resource
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        secretKeyRef:
                                          description: Selects a key of a secret in
                                            the pod's namespace
                                          properties:
                                            key:
                                              description: The key of the secret to
                                                select from.  Must be a valid secret
                                                key.
                                              type: string
                                            name:
                                              default: ""
                                              description: |-
                                                Name of the referent.
                                                This field is effectively required, but due to backwards compatibility is
                                                allowed to be empty. Instances of this type with an empty value here are
                                                almost certainly wrong.
                                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                              type: string
                                            optional:
                                              description: Specify whether the Secret
                                                or its key must be defined
                                              type: boolean
                                          required:
                                          - key
                                          type: object
                                          x-kubernetes-map-type: atomic
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              resources:
                                description: Resources specifies the resource requirements
                                  for the container.
                                properties:
                                  claims:
                                    description: |-
                                      Claims lists the names of resources, defined in spec.resourceClaims,
                                      that are used by this container.

                                      This field depends on the
                                      DynamicResourceAllocation feature gate.

                                      This field is immutable. It can only be set for containers.
                                    items:
                                      description: ResourceClaim references one entry
                                        in PodSpec.ResourceClaims.
                                      properties:
                                        name:
                                          description: |-
                                            Name must match the name of one entry in pod.spec.resourceClaims of
                                            the Pod where this field is used. It makes that resource available
                                            inside a container.
                                          type: string
                                        request:
                                          description: |-
                                            Request is the name chosen for a request in the referenced claim.
                                            If empty, everything from the claim is made available, otherwise
                                            only the result of this request.
                                          type: string
                                      required:
                                      - name
                                      type: object
                                    type: array
                                    x-kubernetes-list-map-keys:
                                    - name
                                    x-kubernetes-list-type: map
                                  limits:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: |-
                                      Limits describes the maximum amount of compute resources allowed.
                                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    type: object
                                  requests:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: |-
                                      Requests describes the minimum amount of compute resources required.
                                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    type: object
                                type: object
                            type: object
                          replicas:
                            default: 1
                            description: Replicas is the number of replicas for the
                              dex deployment.
                            format: int32
                            minimum: 1
                            type: integer
                        type: object
                      ingress:
                        description: |-
                          Ingress defines the ingress configuration for KonfluxUI.
                          This affects the proxy, oauth2-proxy, and dex components.
                        nullable: true
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: Annotations to add to the ingress resource.
                            type: object
                          enabled:
                            description: |-
                              Enabled controls whether an Ingress resource should be created.
                              When nil (unset), defaults to true on OpenShift, false otherwise.
                            nullable: true
                            type: boolean
                          host:
                            description: |-
                              Host is the hostname used as the endpoint for configuring oauth2-proxy, dex, and related components.
                              When set, this hostname is always used regardless of whether ingress is enabled,
                              allowing users who manage their own external routing (e.g., Gateway API, hardware LB)
                              to configure the endpoint without the operator managing an Ingress resource.
                              On OpenShift, if empty, the default ingress domain and naming convention will be used.
                            type: string
                          ingressClassName:
                            description: IngressClassName specifies which IngressClass
                              to use for the ingress.
                            type: string
                          nodePortService:
                            description: |-
                              NodePortService configures the proxy Service as a NodePort type.
                              When set, the proxy Service will be exposed via NodePort instead of ClusterIP.
                              This is useful for accessing Konflux UI from outside the cluster without an Ingress controller.
                            properties:
                              httpsPort:
                                description: |-
                                  HTTPSPort is the NodePort to use for the HTTPS port.
                                  If not specified, Kubernetes will allocate a port automatically.
                                  This is useful for exposing Konflux UI to the outside world without an Ingress controller.
                                format: int32
                                maximum: 32767
                                minimum: 30000
                                type: integer
                            type: object
                          tlsSecretName:
                            description: |-
                              TLSSecretName is the name of the Kubernetes TLS secret to use for the ingress.
                              If not specified, TLS will not be configured on the ingress.
                            type: string
                        type: object
                      proxy:
                        description: Proxy defines customizations for the proxy deployment.
                        properties:
                          endpoints:
                            description: |-
                              Endpoints configures optional backend services that the proxy routes to.
                              Each endpoint can be independently enabled and customized.
                            properties:
                              kite:
                                description: |-
                                  Kite enables the Kite plugin endpoint.
                                  When enabled, requests to /api/k8s/plugins/kite/ are proxied to the Kite backend.
                                properties:
                                  enabled:
                                    description: Enabled controls whether this endpoint
                                      is active.
                                    type: boolean
                                  hostname:
                                    description: Hostname overrides the default backend
                                      service address.
                                    type: string
                                required:
                                - enabled
                                type: object
                              kubearchive:
                                description: |-
                                  KubeArchive enables the KubeArchive plugin endpoint.
                                  When enabled, requests to /api/k8s/plugins/kubearchive/ are proxied to the KubeArchive backend.
                                properties:
                                  enabled:
                                    description: Enabled controls whether this endpoint
                                      is active.
                                    type: boolean
                                  hostname:
                                    description: Hostname overrides the default backend
                                      service address.
                                    type: string
                                required:
                                - enabled
                                type: object
                              watson:
                                description: |-
                                  Watson enables the Watson chatbot endpoint.
                                  When enabled, requests to /api/chatbot/ are proxied to the IBM Watson Assistant API.
                                properties:
                                  enabled:
                                    description: Enabled controls whether the Watson
                                      chatbot endpoint is active.
                                    type: boolean
                                  hostname:
                                    description: |-
                                      Hostname overrides the Watson API host.
                                      Defaults to api.us-east.assistant.watson.cloud.ibm.com.
                                    type: string
                                  secretName:
                                    default: watson-api-key
                                    description: |-
                                      SecretName is the name of the Secret containing the Watson API key.
                                      The Secret must have a key named API_KEY with the pre-encoded Basic auth
                                      value (e.g. base64("apikey:<your-api-key>")).
                                    minLength: 1
                                    type: string
                                required:
                                - enabled
                                type: object
                            type: object
                          oauth2Proxy:
                            description: OAuth2Proxy defines customizations for the
                              oauth2-proxy container.
                            properties:
                              env:
                                description: Env specifies environment variables for
                                  the container.
                                items:
                                  description: EnvVar represents an environment variable
                                    present in a Container.
                                  properties:
                                    name:
                                      description: |-
                                        Name of the environment variable.
                                        May consist of any printable ASCII characters except '='.
                                      type: string
                                    value:
                                      description: |-
                                        Variable references $(VAR_NAME) are expanded
                                        using the previously defined environment variables in the container and
                                        any service environment variables. If a variable cannot be resolved,
                                        the reference in the input string will be unchanged. Double $$ are reduced
                                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                        Escaped references will never be expanded, regardless of whether the variable
                                        exists or not.
                                        Defaults to "".
                                      type: string
                                    valueFrom:
                                      description: Source for the environment variable's
                                        value. Cannot be used if value is not empty.
                                      properties:
                                        configMapKeyRef:
                                          description: Selects a key of a ConfigMap.
                                          properties:
                                            key:
                                              description: The key to select.
                                              type: string
                                            name:
                                              default: ""
                                              description: |-
                                                Name of the referent.
                                                This field is effectively required, but due to backwards compatibility is
                                                allowed to be empty. Instances of this type with an empty value here are
                                                almost certainly wrong.
                                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                              type: string
                                            optional:
                                              description: Specify whether the ConfigMap
                                                or its key must be defined
                                              type: boolean
                                          required:
                                          - key
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        fieldRef:
                                          description: |-
                                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                          properties:
                                            apiVersion:
                                              description: Version of the schema the
                                                FieldPath is written in terms of,
                                                defaults to "v1".
                                              type: string
                                            fieldPath:
                                              description: Path of the field to select
                                                in the specified API version.
                                              type: string
                                          required:
                                          - fieldPath
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        fileKeyRef:
                                          description: |-
                                            FileKeyRef selects a key of the env file.
                                            Requires the EnvFiles feature gate to be enabled.
                                          properties:
                                            key:
                                              description: |-
                                                The key within the env file. An invalid key will prevent the pod from starting.
                                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                              type: string
                                            optional:
                                              default: false
                                              description: |-
                                                Specify whether the file or its key must be defined. If the file or key
                                                does not exist, then the env var is not published.
                                                If optional is set to true and the specified key does not exist,
                                                the environment variable will not be set in the Pod's containers.

                                                If optional is set to false and the specified key does not exist,
                                                an error will be returned during Pod creation.
                                              type: boolean
                                            path:
                                              description: |-
                                                The path within the volume from which to select the file.
                                                Must be relative and may not contain the '..' path or start with '..'.
                                              type: string
                                            volumeName:
                                              description: The name of the volume
                                                mount containing the env file.
                                              type: string
                                          required:
                                          - key
                                          - path
                                          - volumeName
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        resourceFieldRef:
                                          description: |-
                                            Selects a resource of the container: only resources limits and requests
                                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                          properties:
                                            containerName:
                                              description: 'Container name: required
                                                for volumes, optional for env vars'
                                              type: string
                                            divisor:
                                              anyOf:
                                              - type: integer
                                              - type: string
                                              description: Specifies the output format
                                                of the exposed resources, defaults
                                                to "1"
                                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                              x-kubernetes-int-or-string: true
                                            resource:
                                              description: 'Required: resource to
                                                select'
                                              type: string
                                          required:
                                          - resource
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        secretKeyRef:
                                          description: Selects a key of a secret in
                                            the pod's namespace
                                          properties:
                                            key:
                                              description: The key of the secret to
                                                select from.  Must be a valid secret
                                                key.
                                              type: string
                                            name:
                                              default: ""
                                              description: |-
                                                Name of the referent.
                                                This field is effectively required, but due to backwards compatibility is
                                                allowed to be empty. Instances of this type with an empty value here are
                                                almost certainly wrong.
                                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                              type: string
                                            optional:
                                              description: Specify whether the Secret
                                                or its key must be defined
                                              type: boolean
                                          required:
                                          - key
                                          type: object
                                          x-kubernetes-map-type: atomic
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              resources:
                                description: Resources specifies the resource requirements
                                  for the container.
                                properties:
                                  claims:
                                    description: |-
                                      Claims lists the names of resources, defined in spec.resourceClaims,
                                      that are used by this container.

                                      This field depends on the
                                      DynamicResourceAllocation feature gate.

                                      This field is immutable. It can only be set for containers.
                                    items:
                                      description: ResourceClaim references one entry
                                        in PodSpec.ResourceClaims.
                                      properties:
                                        name:
                                          description: |-
                                            Name must match the name of one entry in pod.spec.resourceClaims of
                                            the Pod where this field is used. It makes that resource available
                                            inside a container.
                                          type: string
                                        request:
                                          description: |-
                                            Request is the name chosen for a request in the referenced claim.
                                            If empty, everything from the claim is made available, otherwise
                                            only the result of this request.
                                          type: string
                                      required:
                                      - name
                                      type: object
                                    type: array
                                    x-kubernetes-list-map-keys:
                                    - name
                                    x-kubernetes-list-type: map
                                  limits:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: |-
                                      Limits describes the maximum amount of compute resources allowed.
                                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    type: object
                                  requests:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: |-
                                      Requests describes the minimum amount of compute resources required.
                                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    type: object
                                type: object
                            type: object
                          replicas:
                            default: 1
                            description: Replicas is the number of replicas for the
                              proxy deployment.
                            format: int32
                            minimum: 1
                            type: integer
                          reverseProxy:
                            description: ReverseProxy defines customizations for the
                              reverse proxy container.
                            properties:
                              env:
                                description: Env specifies environment variables for
                                  the container.
                                items:
                                  description: EnvVar represents an environment variable
                                    present in a Container.
                                  properties:
                                    name:
                                      description: |-
                                        Name of the environment variable.
                                        May consist of any printable ASCII characters except '='.
                                      type: string
                                    value:
                                      description: |-
                                        Variable references $(VAR_NAME) are expanded
                                        using the previously defined environment variables in the container and
                                        any service environment variables. If a variable cannot be resolved,
                                        the reference in the input string will be unchanged. Double $$ are reduced
                                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                        Escaped references will never be expanded, regardless of whether the variable
                                        exists or not.
                                        Defaults to "".
                                      type: string
                                    valueFrom:
                                      description: Source for the environment variable's
                                        value. Cannot be used if value is not empty.
                                      properties:
                                        configMapKeyRef:
                                          description: Selects a key of a ConfigMap.
                                          properties:
                                            key:
                                              description: The key to select.
                                              type: string
                                            name:
                                              default: ""
                                              description: |-
                                                Name of the referent.
                                                This field is effectively required, but due to backwards compatibility is
                                                allowed to be empty. Instances of this type with an empty value here are
                                                almost certainly wrong.
                                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                              type: string
                                            optional:
                                              description: Specify whether the ConfigMap
                                                or its key must be defined
                                              type: boolean
                                          required:
                                          - key
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        fieldRef:
                                          description: |-
                                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                          properties:
                                            apiVersion:
                                              description: Version of the schema the
                                                FieldPath is written in terms of,
                                                defaults to "v1".
                                              type: string
                                            fieldPath:
                                              description: Path of the field to select
                                                in the specified API version.
                                              type: string
                                          required:
                                          - fieldPath
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        fileKeyRef:
                                          description: |-
                                            FileKeyRef selects a key of the env file.
                                            Requires the EnvFiles feature gate to be enabled.
                                          properties:
                                            key:
                                              description: |-
                                                The key within the env file. An invalid key will prevent the pod from starting.
                                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                              type: string
                                            optional:
                                              default: false
                                              description: |-
                                                Specify whether the file or its key must be defined. If the file or key
                                                does not exist, then the env var is not published.
                                                If optional is set to true and the specified key does not exist,
                                                the environment variable will not be set in the Pod's containers.

                                                If optional is set to false and the specified key does not exist,
                                                an error will be returned during Pod creation.
                                              type: boolean
                                            path:
                                              description: |-
                                                The path within the volume from which to select the file.
                                                Must be relative and may not contain the '..' path or start with '..'.
                                              type: string
                                            volumeName:
                                              description: The name of the volume
                                                mount containing the env file.
                                              type: string
                                          required:
                                          - key
                                          - path
                                          - volumeName
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        resourceFieldRef:
                                          description: |-
                                            Selects a resource of the container: only resources limits and requests
                                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                          properties:
                                            containerName:
                                              description: 'Container name: required
                                                for volumes, optional for env vars'
                                              type: string
                                            divisor:
                                              anyOf:
                                              - type: integer
                                              - type: string
                                              description: Specifies the output format
                                                of the exposed resources, defaults
                                                to "1"
                                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                              x-kubernetes-int-or-string: true
                                            resource:
                                              description: 'Required: resource to
                                                select'
                                              type: string
                                          required:
                                          - resource
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        secretKeyRef:
                                          description: Selects a key of a secret in
                                            the pod's namespace
                                          properties:
                                            key:
                                              description: The key of the secret to
                                                select from.  Must be a valid secret
                                                key.
                                              type: string
                                            name:
                                              default: ""
                                              description: |-
                                                Name of the referent.
                                                This field is effectively required, but due to backwards compatibility is
                                                allowed to be empty. Instances of this type with an empty value here are
                                                almost certainly wrong.
                                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                              type: string
                                            optional:
                                              description: Specify whether the Secret
                                                or its key must be defined
                                              type: boolean
                                          required:
                                          - key
                                          type: object
                                          x-kubernetes-map-type: atomic
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              resources:
                                description: Resources specifies the resource requirements
                                  for the container.
                                properties:
                                  claims:
                                    description: |-
                                      Claims lists the names of resources, defined in spec.resourceClaims,
                                      that are used by this container.

                                      This field depends on the
                                      DynamicResourceAllocation feature gate.

                                      This field is immutable. It can only be set for containers.
                                    items:
                                      description: ResourceClaim references one entry
                                        in PodSpec.ResourceClaims.
                                      properties:
                                        name:
                                          description: |-
                                            Name must match the name of one entry in pod.spec.resourceClaims of
                                            the Pod where this field is used. It makes that resource available
                                            inside a container.
                                          type: string
                                        request:
                                          description: |-
                                            Request is the name chosen for a request in the referenced claim.
                                            If empty, everything from the claim is made available, otherwise
                                            only the result of this request.
                                          type: string
                                      required:
                                      - name
                                      type: object
                                    type: array
                                    x-kubernetes-list-map-keys:
                                    - name
                                    x-kubernetes-list-type: map
                                  limits:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: |-
                                      Limits describes the maximum amount of compute resources allowed.
                                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    type: object
                                  requests:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: |-
                                      Requests describes the minimum amount of compute resources required.
                                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    type: object
                                type: object
                            type: object
                        type: object
                      runtimeConfig:
                        description: |-
                          RuntimeConfig defines frontend runtime configuration for the Konflux UI.
                          These settings are injected as window.KONFLUX_RUNTIME properties in the SPA.
                        properties:
                          chatBot:
                            description: ChatBot configures the AI chatbot feature
                              in the UI.
                            properties:
                              enabled:
                                description: Enabled controls whether the chatbot
                                  UI is visible to users.
                                type: boolean
                            type: object
                          monitoring:
                            description: Monitoring configures error monitoring (e.g.
                              Sentry) for the UI frontend.
                            properties:
                              cluster:
                                description: Cluster identifies the cluster name for
                                  error reports.
                                type: string
                              dsn:
                                description: DSN is the data source name (e.g. Sentry
                                  DSN) for error reporting.
                                type: string
                              enabled:
                                description: Enabled controls whether error monitoring
                                  is active.
                                type: boolean
                              environment:
                                description: Environment identifies the deployment
                                  environment (e.g. "staging", "production").
                                type: string
                              sampleRateErrors:
                                description: SampleRateErrors controls the error event
                                  sample rate (0.0 to 1.0).
                                pattern: ^(0(\.\d+)?|1(\.0+)?)$
                                type: string
                            type: object
                        type: object
                    type: object
                type: object
            type: object
          status:
            description: KonfluxStatus defines the observed state of Konflux.
            properties:
              components:
                description: Components shows the status of individual Konflux components
                items:
                  description: ComponentStatus represents the status of a Konflux
                    component.
                  properties:
                    message:
                      description: Message provides additional information about the
                        component status
                      type: string
                    name:
                      description: Name of the component
                      type: string
                    ready:
                      description: Ready indicates if the component is ready
                      type: boolean
                  required:
                  - name
                  - ready
                  type: object
                type: array
              conditions:
                description: Conditions represent the latest available observations
                  of the Konflux state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              uiURL:
                description: |-
                  UIURL is the URL to access the Konflux UI.
                  This is populated from the KonfluxUI status when ingress is enabled.
                type: string
            type: object
        type: object
        x-kubernetes-validations:
        - message: Konflux CR must be named 'konflux'. Only one instance is allowed
            per cluster.
          rule: self.metadata.name == 'konflux'
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
