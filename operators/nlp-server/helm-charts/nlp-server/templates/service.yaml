apiVersion: v1
kind: Service
metadata:
  name: {{ include "nlp-server.fullname" . }}
  labels:
    {{- include "nlp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nlp-server.selectorLabels" . | nindent 4 }}
