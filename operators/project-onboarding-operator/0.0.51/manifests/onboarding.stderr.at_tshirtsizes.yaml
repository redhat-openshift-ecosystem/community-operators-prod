apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: tshirtsizes.onboarding.stderr.at
spec:
  group: onboarding.stderr.at
  names:
    kind: TShirtSize
    listKind: TShirtSizeList
    plural: tshirtsizes
    shortNames:
    - tts
    singular: tshirtsize
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.referencedBy
      name: Refs
      type: integer
    - jsonPath: .spec.description
      name: Description
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: TShirtSize is a cluster-scoped catalogue entry to pre-define
          project sizes (S, M, L, ...).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TShirtSizeSpec defines quota and limit presets for a named
              project size.
            properties:
              description:
                description: Description is a human-readable summary of this size.
                type: string
              limitRanges:
                description: LimitRanges are applied to namespaces that reference
                  this T-shirt size.
                properties:
                  container:
                    description: Container sets LimitRange defaults, default requests,
                      and min/max for containers.
                    properties:
                      default:
                        description: Default sets default CPU and memory limits applied
                          to containers without limits.
                        properties:
                          cpu:
                            description: CPU quantity (e.g. 4, 500m).
                            type: string
                          memory:
                            description: Memory quantity (e.g. 4Gi, 500Mi).
                            type: string
                        type: object
                      defaultRequest:
                        description: DefaultRequest sets default CPU and memory requests
                          applied to containers without requests.
                        properties:
                          cpu:
                            description: CPU quantity (e.g. 4, 500m).
                            type: string
                          memory:
                            description: Memory quantity (e.g. 4Gi, 500Mi).
                            type: string
                        type: object
                      max:
                        description: Max sets maximum CPU and memory per container.
                        properties:
                          cpu:
                            description: CPU quantity (e.g. 4, 500m).
                            type: string
                          memory:
                            description: Memory quantity (e.g. 4Gi, 500Mi).
                            type: string
                        type: object
                      min:
                        description: Min sets minimum CPU and memory per container.
                        properties:
                          cpu:
                            description: CPU quantity (e.g. 4, 500m).
                            type: string
                          memory:
                            description: Memory quantity (e.g. 4Gi, 500Mi).
                            type: string
                        type: object
                    type: object
                  enabled:
                    description: Enabled turns LimitRange reconciliation on or off
                      for this namespace entry.
                    type: boolean
                  pod:
                    description: Pod sets LimitRange constraints for Pod-type limits
                      (sum of containers in a pod).
                    properties:
                      max:
                        description: Max sets maximum CPU and memory for a pod (sum
                          of its containers).
                        properties:
                          cpu:
                            description: CPU quantity (e.g. 4, 500m).
                            type: string
                          memory:
                            description: Memory quantity (e.g. 4Gi, 500Mi).
                            type: string
                        type: object
                      min:
                        description: Min sets minimum CPU and memory for a pod (sum
                          of its containers).
                        properties:
                          cpu:
                            description: CPU quantity (e.g. 4, 500m).
                            type: string
                          memory:
                            description: Memory quantity (e.g. 4Gi, 500Mi).
                            type: string
                        type: object
                    type: object
                  pvc:
                    description: PVC sets min/max storage bounds for PersistentVolumeClaim-type
                      limits.
                    properties:
                      max:
                        description: Max sets maximum storage for PVC-type limits.
                        properties:
                          storage:
                            description: Storage quantity (e.g. 1Gi, 20Gi).
                            type: string
                        type: object
                      min:
                        description: Min sets minimum storage for PVC-type limits.
                        properties:
                          storage:
                            description: Storage quantity (e.g. 1Gi, 20Gi).
                            type: string
                        type: object
                    type: object
                type: object
              resourceQuotas:
                description: ResourceQuotas are applied to namespaces that reference
                  this T-shirt size.
                properties:
                  configMaps:
                    description: ConfigMaps is the maximum number of ConfigMaps.
                    format: int32
                    type: integer
                  cpu:
                    description: CPU is the total CPU quota for all pods (e.g. "4"
                      or "4000m").
                    type: string
                  enabled:
                    description: Enabled turns ResourceQuota reconciliation on or
                      off for this namespace entry.
                    type: boolean
                  ephemeralStorage:
                    description: EphemeralStorage is the total ephemeral storage quota
                      for all pods (e.g. "4Gi"). Lower-case suffixes (gi, mi) are
                      normalized automatically.
                    type: string
                  limits:
                    description: Limits sets cluster-wide limits.* ResourceQuota keys.
                    properties:
                      cpu:
                        description: CPU is the limits.cpu quota (e.g. "4").
                        type: string
                      ephemeralStorage:
                        description: EphemeralStorage is the limits.ephemeral-storage
                          quota (e.g. "4Mi"). Lower-case suffixes are normalized automatically.
                        type: string
                      memory:
                        description: Memory is the limits.memory quota (e.g. "4Gi").
                          Lower-case suffixes are normalized automatically.
                        type: string
                    type: object
                  memory:
                    description: Memory is the total memory quota for all pods (e.g.
                      "4Gi").
                    type: string
                  persistentVolumeClaims:
                    description: PersistentVolumeClaims is the maximum number of PersistentVolumeClaims.
                    format: int32
                    type: integer
                  pods:
                    description: Pods is the maximum number of pods in the namespace.
                    format: int32
                    type: integer
                  replicationControllers:
                    description: ReplicationControllers is the maximum number of ReplicationControllers.
                    format: int32
                    type: integer
                  requests:
                    description: Requests sets cluster-wide requests.* ResourceQuota
                      keys.
                    properties:
                      cpu:
                        description: CPU is the requests.cpu quota (e.g. "1").
                        type: string
                      ephemeralStorage:
                        description: EphemeralStorage is the requests.ephemeral-storage
                          quota (e.g. "2Gi").
                        type: string
                      memory:
                        description: Memory is the requests.memory quota (e.g. "2Gi").
                        type: string
                      storage:
                        description: Storage is the requests.storage quota (e.g. "50Gi").
                        type: string
                    type: object
                  resourceQuotas:
                    description: ResourceQuotas is the maximum number of ResourceQuota
                      objects in the namespace.
                    format: int32
                    type: integer
                  secrets:
                    description: Secrets is the maximum number of Secrets.
                    format: int32
                    type: integer
                  services:
                    description: Services is the maximum number of Services.
                    format: int32
                    type: integer
                  storageClasses:
                    description: StorageClasses lists StorageClass quota keys and
                      values for ResourceQuota hard limits.
                    items:
                      description: StorageClassQuota is one StorageClass-related ResourceQuota
                        hard limit key/value pair.
                      properties:
                        key:
                          description: Key is the ResourceQuota hard limit name, e.g.
                            bronze.storageclass.storage.k8s.io/requests.storage.
                          minLength: 1
                          type: string
                        value:
                          description: Value is the quota value (e.g. 10Gi or 10).
                          type: string
                      required:
                      - key
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - key
                    x-kubernetes-list-type: map
                type: object
            type: object
            x-kubernetes-validations:
            - message: at least one of resourceQuotas or limitRanges must be set
              rule: has(self.resourceQuotas) || has(self.limitRanges)
          status:
            description: TShirtSizeStatus defines the observed state of TShirtSize.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              phase:
                type: string
              referencedBy:
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
