apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  creationTimestamp: null
  name: disconnectedplatforms.mirror.mirror.mathianasj.github.com
spec:
  group: mirror.mirror.mathianasj.github.com
  names:
    kind: DisconnectedPlatform
    listKind: DisconnectedPlatformList
    plural: disconnectedplatforms
    shortNames:
    - dp
    singular: disconnectedplatform
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: DisconnectedPlatform is the top-level orchestrator for managing
          disconnected OpenShift environments
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DisconnectedPlatformSpec defines the desired state of DisconnectedPlatform
            properties:
              airgapped:
                description: Airgapped mode configuration for importing and serving
                  mirrored content
                properties:
                  acm:
                    description: Red Hat Advanced Cluster Management configuration
                      for airgapped multi-cluster management
                    properties:
                      enabled:
                        description: Enable ACM operator installation and MultiClusterHub
                          creation
                        type: boolean
                      hostInventory:
                        description: Host inventory configuration for assisted installer
                          bare-metal provisioning
                        properties:
                          credential:
                            description: ACM infrastructure provider credential configuration
                            properties:
                              baseDomain:
                                description: Base domain for cluster deployments
                                type: string
                              enabled:
                                default: true
                                description: Enable creation of the ACM infrastructure
                                  provider credential
                                type: boolean
                              name:
                                default: mirror-operator-credential
                                description: Name of the credential Secret
                                type: string
                              namespace:
                                default: multicluster-engine
                                description: Namespace where the credential Secret
                                  will be created
                                type: string
                            type: object
                          databaseStorageSize:
                            default: 200Gi
                            description: Storage size for AgentServiceConfig databaseStorage
                              PVC
                            type: string
                          enabled:
                            description: Enable host inventory and assisted installer
                              support
                            type: boolean
                          filesystemStorageSize:
                            default: 200Gi
                            description: Storage size for AgentServiceConfig filesystemStorage
                              PVC
                            type: string
                          imageStorageSize:
                            default: 100Gi
                            description: Storage size for AgentServiceConfig imageStorage
                              PVC
                            type: string
                          infraEnv:
                            description: InfraEnv configuration for discovery ISO
                              generation
                            properties:
                              additionalNTPSources:
                                description: Additional NTP sources for cluster hosts
                                items:
                                  type: string
                                type: array
                              cpuArchitecture:
                                default: x86_64
                                description: CPU architecture for discovery image
                                type: string
                              enabled:
                                default: true
                                description: Enable InfraEnv creation
                                type: boolean
                              imageType:
                                default: full-iso
                                description: Discovery image type
                                enum:
                                - full-iso
                                - minimal-iso
                                type: string
                              namespace:
                                default: mirror-operator-system
                                description: Namespace where the InfraEnv will be
                                  created
                                type: string
                              networkType:
                                default: dhcp
                                description: Network configuration type
                                enum:
                                - dhcp
                                - static
                                type: string
                              nmStateConfigLabels:
                                additionalProperties:
                                  type: string
                                description: NMStateConfig label selector for static
                                  networking (only used when networkType=static)
                                type: object
                              sshAuthorizedKey:
                                description: SSH public key for agent debugging (if
                                  empty, reads from cluster MachineConfig)
                                type: string
                            type: object
                          rhcosImage:
                            description: |-
                              Override the full RHCOS server container image reference (e.g. "my-registry:8443/rhcos-server:4.21")
                              When set, this is used instead of constructing the image from mirrorRegistry + version
                            type: string
                          storageClass:
                            description: Kubernetes StorageClass for AgentServiceConfig
                              PVCs
                            type: string
                          versions:
                            description: OCP versions to configure for host inventory
                              (RHCOS boot images will be served for each)
                            items:
                              description: HostInventoryVersion represents an OCP
                                version for which RHCOS boot images are served
                              properties:
                                cpuArchitecture:
                                  default: x86_64
                                  description: CPU architecture
                                  type: string
                                openshiftVersion:
                                  description: OpenShift version (e.g., "4.18.12")
                                  type: string
                              required:
                              - openshiftVersion
                              type: object
                            type: array
                        type: object
                      multiClusterHub:
                        description: MultiClusterHub configuration
                        properties:
                          customCAConfigMap:
                            description: Custom CA bundle ConfigMap name for the mirror
                              registry TLS
                            type: string
                          disableHubSelfManagement:
                            description: Disable default self-management of the hub
                              cluster
                            type: boolean
                          imagePullSecret:
                            description: |-
                              Reference to the image pull secret for the mirror registry
                              If empty, uses the platform's airgapped registry credentials
                            properties:
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                      subscription:
                        description: OLM subscription overrides for ACM in the airgapped
                          catalog
                        properties:
                          approvalStrategy:
                            default: Automatic
                            description: OLM install plan approval strategy
                            enum:
                            - Automatic
                            - Manual
                            type: string
                          catalogSource:
                            description: OLM catalog source name
                            type: string
                          catalogSourceNamespace:
                            description: OLM catalog source namespace
                            type: string
                          channel:
                            description: OLM subscription channel
                            type: string
                          disabled:
                            description: Set to true to skip installation of this
                              operator
                            type: boolean
                          startingCSV:
                            description: Starting CSV version for the subscription
                            type: string
                        type: object
                    type: object
                  bootstrapEnabled:
                    description: Enable cluster bootstrapping from mirrored content
                    type: boolean
                  defaultImageSetConfig:
                    description: Default ImageSetConfiguration YAML for auto-imported
                      bundles
                    type: string
                  importPath:
                    description: Filesystem path for physical media import (e.g. /mnt/physical-media)
                    type: string
                  importScanSchedule:
                    default: '*/30 * * * *'
                    description: Cron schedule for scanning importPath for new bundles
                      (e.g. '*/15 * * * *')
                    type: string
                  managementCluster:
                    description: Whether this cluster serves as the management hub
                    type: boolean
                  mirrorRegistry:
                    description: Target mirror registry URL for imported content
                    type: string
                  mirrorRegistryConfig:
                    description: Mirror registry deployment configuration (for operator-managed
                      registry)
                    properties:
                      dataPath:
                        default: /opt/quay
                        description: Storage path for Quay data
                        type: string
                      port:
                        default: 8443
                        description: Quay service port
                        format: int32
                        minimum: 1
                        type: integer
                    type: object
                  quay:
                    description: Quay registry configuration for the airgapped cluster
                    properties:
                      adminPassword:
                        description: Quay admin password
                        type: string
                      adminUser:
                        description: Quay admin username
                        type: string
                      clair:
                        description: Clair vulnerability scanner configuration
                        properties:
                          useRedHatVEXOnly:
                            description: Configure Clair to use only Red Hat VEX data
                              for vulnerability scanning
                            type: boolean
                        type: object
                      enabled:
                        default: true
                        description: Deploy and manage a Quay registry on the airgapped
                          cluster
                        type: boolean
                      organizationName:
                        default: mirror
                        description: Quay organization name for mirrored content
                        type: string
                      replicas:
                        description: |-
                          Number of replicas for Quay, Clair, and Mirror components.
                          If unset, auto-detects Single Node OpenShift and defaults to 1.
                        format: int32
                        minimum: 1
                        type: integer
                      storage:
                        description: Storage configuration for airgapped Quay (filesystem-backed)
                        properties:
                          size:
                            default: 500Gi
                            description: Storage size for the Quay registry PVC
                            type: string
                          storageClass:
                            description: Kubernetes StorageClass for the Quay registry
                              PVC
                            type: string
                        type: object
                    type: object
                  registryCredentials:
                    description: Reference to a Secret containing registry pull/push
                      credentials
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  rhtas:
                    description: Red Hat Trusted Artifact Signer configuration for
                      airgapped signature verification
                    properties:
                      trustedRootKeys:
                        description: Reference to a Secret containing trusted root
                          keys for airgapped signature verification
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  verifyPublicKey:
                    description: Cosign public key secret for verifying bundle signatures
                      during auto-import
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              architect:
                description: Airgap Architect web UI configuration
                properties:
                  backendImage:
                    description: Container image for the Architect backend API
                    type: string
                  consolePlugin:
                    description: OpenShift console plugin configuration for integrated
                      UI
                    properties:
                      enabled:
                        description: Enable the OpenShift console plugin for integrated
                          management UI
                        type: boolean
                      image:
                        description: Container image for the console plugin
                        type: string
                    type: object
                  enabled:
                    description: Deploy the Airgap Architect web UI
                    type: boolean
                  frontendImage:
                    description: Container image for the Architect frontend
                    type: string
                  githubTokenSecret:
                    description: Reference to a Secret containing a GitHub token for
                      release lookups
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  pullSecret:
                    description: Reference to a Secret containing pull credentials
                      for Architect images
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  replicas:
                    default: 1
                    description: Number of replicas for the Architect deployment
                    format: int32
                    minimum: 1
                    type: integer
                  route:
                    description: OpenShift Route configuration for external access
                    properties:
                      host:
                        description: Custom hostname for the OpenShift Route
                        type: string
                      tls:
                        description: TLS configuration for the Route
                        properties:
                          termination:
                            default: edge
                            description: Route TLS termination type
                            enum:
                            - edge
                            - passthrough
                            - reencrypt
                            type: string
                        type: object
                    type: object
                type: object
              connected:
                description: Connected mode configuration for content collection and
                  mirroring
                properties:
                  artifactStorage:
                    description: Storage configuration for collection artifacts
                    properties:
                      size:
                        default: 200Gi
                        description: Storage size for collection artifacts (e.g. '200Gi')
                        type: string
                      storageClass:
                        description: Kubernetes StorageClass to use for artifact PVCs
                        type: string
                    required:
                    - size
                    type: object
                  certIssuer:
                    description: cert-manager issuer reference for automatic TLS certificate
                      provisioning (used by managed Keycloak)
                    properties:
                      kind:
                        default: ClusterIssuer
                        description: Kind of the cert-manager issuer resource
                        enum:
                        - ClusterIssuer
                        - Issuer
                        type: string
                      name:
                        description: Name of the cert-manager Issuer or ClusterIssuer
                        type: string
                    required:
                    - name
                    type: object
                  collectionSchedule:
                    description: Cron schedule for automated content collection (e.g.
                      '0 2 * * 0' for weekly Sunday 2am). Required only when triggerTypes
                      includes 'scheduled'.
                    type: string
                  mirrorRegistry:
                    description: Target mirror registry URL (auto-configured when
                      managed Quay is enabled)
                    type: string
                  operators:
                    description: OLM operator subscriptions managed by this platform
                    properties:
                      keycloak:
                        description: Red Hat Build of Keycloak operator subscription
                        properties:
                          approvalStrategy:
                            default: Automatic
                            description: OLM install plan approval strategy
                            enum:
                            - Automatic
                            - Manual
                            type: string
                          catalogSource:
                            description: OLM catalog source name
                            type: string
                          catalogSourceNamespace:
                            description: OLM catalog source namespace
                            type: string
                          channel:
                            description: OLM subscription channel
                            type: string
                          disabled:
                            description: Set to true to skip installation of this
                              operator
                            type: boolean
                          startingCSV:
                            description: Starting CSV version for the subscription
                            type: string
                        type: object
                      openshiftPipelines:
                        description: OpenShift Pipelines operator subscription (provides
                          Tekton)
                        properties:
                          approvalStrategy:
                            default: Automatic
                            description: OLM install plan approval strategy
                            enum:
                            - Automatic
                            - Manual
                            type: string
                          catalogSource:
                            description: OLM catalog source name
                            type: string
                          catalogSourceNamespace:
                            description: OLM catalog source namespace
                            type: string
                          channel:
                            description: OLM subscription channel
                            type: string
                          disabled:
                            description: Set to true to skip installation of this
                              operator
                            type: boolean
                          startingCSV:
                            description: Starting CSV version for the subscription
                            type: string
                        type: object
                      osus:
                        description: OpenShift Update Service (OSUS/Cincinnati) operator
                          subscription
                        properties:
                          approvalStrategy:
                            default: Automatic
                            description: OLM install plan approval strategy
                            enum:
                            - Automatic
                            - Manual
                            type: string
                          catalogSource:
                            description: OLM catalog source name
                            type: string
                          catalogSourceNamespace:
                            description: OLM catalog source namespace
                            type: string
                          channel:
                            description: OLM subscription channel
                            type: string
                          disabled:
                            description: Set to true to skip installation of this
                              operator
                            type: boolean
                          startingCSV:
                            description: Starting CSV version for the subscription
                            type: string
                        type: object
                      quayOperator:
                        description: Quay operator subscription for managed registry
                        properties:
                          approvalStrategy:
                            default: Automatic
                            description: OLM install plan approval strategy
                            enum:
                            - Automatic
                            - Manual
                            type: string
                          catalogSource:
                            description: OLM catalog source name
                            type: string
                          catalogSourceNamespace:
                            description: OLM catalog source namespace
                            type: string
                          channel:
                            description: OLM subscription channel
                            type: string
                          disabled:
                            description: Set to true to skip installation of this
                              operator
                            type: boolean
                          startingCSV:
                            description: Starting CSV version for the subscription
                            type: string
                        type: object
                      rhtas:
                        description: Red Hat Trusted Artifact Signer operator subscription
                        properties:
                          approvalStrategy:
                            default: Automatic
                            description: OLM install plan approval strategy
                            enum:
                            - Automatic
                            - Manual
                            type: string
                          catalogSource:
                            description: OLM catalog source name
                            type: string
                          catalogSourceNamespace:
                            description: OLM catalog source namespace
                            type: string
                          channel:
                            description: OLM subscription channel
                            type: string
                          disabled:
                            description: Set to true to skip installation of this
                              operator
                            type: boolean
                          startingCSV:
                            description: Starting CSV version for the subscription
                            type: string
                        type: object
                      rhtpa:
                        description: Red Hat Trusted Profile Analyzer operator subscription
                        properties:
                          approvalStrategy:
                            default: Automatic
                            description: OLM install plan approval strategy
                            enum:
                            - Automatic
                            - Manual
                            type: string
                          catalogSource:
                            description: OLM catalog source name
                            type: string
                          catalogSourceNamespace:
                            description: OLM catalog source namespace
                            type: string
                          channel:
                            description: OLM subscription channel
                            type: string
                          disabled:
                            description: Set to true to skip installation of this
                              operator
                            type: boolean
                          startingCSV:
                            description: Starting CSV version for the subscription
                            type: string
                        type: object
                    type: object
                  quay:
                    description: Quay registry configuration for intermediate image
                      storage
                    properties:
                      externalURL:
                        description: External Quay URL if not using a managed instance
                        type: string
                      managed:
                        description: Deploy and manage a Quay registry instance
                        properties:
                          adminPassword:
                            description: Quay admin password
                            type: string
                          adminUser:
                            description: Quay admin username
                            type: string
                          clair:
                            description: Clair vulnerability scanner configuration
                            properties:
                              useRedHatVEXOnly:
                                description: Configure Clair to use only Red Hat VEX
                                  data for vulnerability scanning
                                type: boolean
                            type: object
                          database:
                            description: External database configuration for Quay
                            properties:
                              host:
                                description: Database hostname
                                type: string
                              name:
                                description: Database name
                                type: string
                              password:
                                description: Database password
                                type: string
                              port:
                                description: Database port
                                format: int32
                                minimum: 1
                                type: integer
                              username:
                                description: Database username
                                type: string
                            required:
                            - host
                            - name
                            type: object
                          enabled:
                            default: true
                            description: Enable managed Quay deployment
                            type: boolean
                          organizationName:
                            default: mirror
                            description: Quay organization name for mirrored content
                            type: string
                          replicas:
                            description: |-
                              Number of replicas for Quay, Clair, and Mirror components.
                              If unset, auto-detects Single Node OpenShift and defaults to 1.
                            format: int32
                            minimum: 1
                            type: integer
                          storage:
                            description: Storage configuration for Quay registry
                            properties:
                              s3AccessKey:
                                description: S3 access key (auto-provisioned via ODF
                                  BucketClaim if not set)
                                type: string
                              s3Bucket:
                                description: S3 bucket name (auto-provisioned via
                                  ODF BucketClaim if not set)
                                type: string
                              s3Endpoint:
                                description: S3 endpoint URL (auto-provisioned via
                                  ODF BucketClaim if not set)
                                type: string
                              s3SecretKey:
                                description: S3 secret key (auto-provisioned via ODF
                                  BucketClaim if not set)
                                type: string
                              size:
                                description: Storage size (for filesystem type)
                                type: string
                              storageClass:
                                description: Kubernetes StorageClass (for filesystem
                                  type)
                                type: string
                              type:
                                default: s3
                                description: 'Storage backend: filesystem (PVC) or
                                  s3 (ODF BucketClaim)'
                                enum:
                                - filesystem
                                - s3
                                type: string
                            required:
                            - type
                            type: object
                        type: object
                    type: object
                  rhcosCollection:
                    description: RHCOS image collection for ACM host inventory in
                      airgapped environments
                    properties:
                      enabled:
                        default: true
                        description: Enable RHCOS image download and server image
                          build in the collection pipeline
                        type: boolean
                      serverBaseImage:
                        default: registry.access.redhat.com/ubi9/nginx-122:latest
                        description: Base image for the RHCOS server container (nginx
                          serving RHCOS files)
                        type: string
                    type: object
                  rhtas:
                    description: Red Hat Trusted Artifact Signer configuration for
                      supply chain security
                    properties:
                      database:
                        description: External database configuration for RHTAS
                        properties:
                          host:
                            description: Database hostname
                            type: string
                          name:
                            description: Database name
                            type: string
                          password:
                            description: Database password
                            type: string
                          port:
                            description: Database port
                            format: int32
                            minimum: 1
                            type: integer
                          username:
                            description: Database username
                            type: string
                        required:
                        - host
                        - name
                        type: object
                      oidc:
                        description: OIDC configuration for Trusted Artifact Signer
                          authentication
                        properties:
                          clientId:
                            description: OIDC client ID
                            type: string
                          clientSecret:
                            description: OIDC client secret
                            type: string
                          issuer:
                            description: OIDC issuer URL
                            type: string
                          managed:
                            description: Managed Keycloak instance for OIDC (auto-provisions
                              Keycloak)
                            properties:
                              adminPassword:
                                description: Keycloak admin password
                                type: string
                              adminUser:
                                description: Keycloak admin username
                                type: string
                              enabled:
                                description: Enable managed Keycloak deployment
                                type: boolean
                              realm:
                                description: Keycloak realm name
                                type: string
                              tlsSecret:
                                description: Reference to a pre-existing TLS secret
                                  for Keycloak
                                properties:
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                          type:
                            description: OIDC identity claim type
                            type: string
                        type: object
                      trustedRootKeys:
                        description: Reference to a Secret containing trusted root
                          keys for signature verification
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  rhtpa:
                    description: Red Hat Trusted Profile Analyzer configuration for
                      SBOM analysis
                    properties:
                      database:
                        description: External database configuration for RHTPA
                        properties:
                          host:
                            description: Database hostname
                            type: string
                          name:
                            description: Database name
                            type: string
                          password:
                            description: Database password
                            type: string
                          username:
                            description: Database username
                            type: string
                        required:
                        - host
                        - name
                        - password
                        - username
                        type: object
                      databaseMaxStorageSize:
                        default: 200Gi
                        description: Maximum size the managed PostgreSQL PVC can auto-expand
                          to when usage exceeds 80%
                        type: string
                      databaseStorageSize:
                        default: 50Gi
                        description: Size of the managed PostgreSQL PVC (ignored when
                          external Database is set)
                        type: string
                      importers:
                        description: Importer sources to enable (redhat-csaf is always
                          enabled)
                        properties:
                          cve:
                            description: Enable CVE importer
                            type: boolean
                          osvGitHub:
                            description: Enable OSV GitHub importer
                            type: boolean
                          redhatSBOMs:
                            description: Enable Red Hat SBOM importer
                            type: boolean
                        type: object
                      oidc:
                        description: OIDC configuration for RHTPA authentication
                        properties:
                          clientId:
                            description: OIDC client ID
                            type: string
                          clientSecret:
                            description: OIDC client secret
                            type: string
                          issuer:
                            description: OIDC issuer URL for RHTPA authentication
                            type: string
                        type: object
                      storage:
                        description: Storage configuration for Trusted Profile Analyzer
                        properties:
                          accessKey:
                            description: S3 access key (auto-provisioned via ODF BucketClaim
                              if not set)
                            type: string
                          bucket:
                            description: S3 bucket name (auto-provisioned via ODF
                              BucketClaim if not set)
                            type: string
                          region:
                            description: S3 region
                            type: string
                          secretKey:
                            description: S3 secret key (auto-provisioned via ODF BucketClaim
                              if not set)
                            type: string
                          size:
                            description: Storage size (for local type)
                            type: string
                          type:
                            default: s3
                            description: 'Storage backend: local (PVC) or s3 (ODF
                              BucketClaim)'
                            enum:
                            - local
                            - s3
                            type: string
                        required:
                        - type
                        type: object
                    type: object
                  triggerTypes:
                    description: 'Collection trigger types: scheduled (cron), manual
                      (on-demand), event (webhook)'
                    items:
                      enum:
                      - scheduled
                      - manual
                      - event
                      type: string
                    type: array
                required:
                - artifactStorage
                type: object
              gitOps:
                description: GitOps integration configuration
                properties:
                  branch:
                    description: Git branch to use
                    type: string
                  credentials:
                    description: Reference to a Secret containing git credentials
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  enabled:
                    description: Enable GitOps integration for declarative management
                    type: boolean
                  path:
                    description: Path within the repository for platform configuration
                    type: string
                  repositoryURL:
                    description: Git repository URL for storing platform configuration
                    type: string
                type: object
              mode:
                allOf:
                - enum:
                  - connected
                  - airgapped
                - enum:
                  - connected
                  - airgapped
                description: 'Platform operating mode: connected (internet access)
                  or airgapped (disconnected)'
                type: string
            required:
            - mode
            type: object
          status:
            properties:
              collectionHistory:
                description: History of past collections
                items:
                  properties:
                    size:
                      description: Total size of collected artifacts
                      type: string
                    status:
                      description: Collection status
                      type: string
                    timestamp:
                      description: Timestamp of the collection
                      format: date-time
                      type: string
                    version:
                      description: Collection version identifier
                      type: string
                  required:
                  - size
                  - status
                  - timestamp
                  - version
                  type: object
                type: array
              components:
                description: Status of managed components
                items:
                  properties:
                    apiGroup:
                      description: API group of the resource (e.g. apps, operators.coreos.com)
                      type: string
                    kind:
                      description: Kubernetes resource kind (e.g. Deployment, Service,
                        Subscription)
                      type: string
                    lastCheck:
                      description: Last health check timestamp
                      format: date-time
                      type: string
                    name:
                      description: Component name
                      type: string
                    namespace:
                      description: Namespace of the managed resource
                      type: string
                    status:
                      description: Component health status
                      type: string
                    url:
                      description: Component URL (if externally accessible)
                      type: string
                  required:
                  - name
                  - status
                  type: object
                type: array
              conditions:
                description: Standard Kubernetes conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              importHistory:
                description: History of past imports
                items:
                  properties:
                    status:
                      description: Import status
                      type: string
                    timestamp:
                      description: Timestamp of the import
                      format: date-time
                      type: string
                    version:
                      description: Import version identifier
                      type: string
                  required:
                  - status
                  - timestamp
                  - version
                  type: object
                type: array
              lastCollection:
                description: Most recent collection information
                properties:
                  size:
                    description: Total size of collected artifacts
                    type: string
                  status:
                    description: Collection status
                    type: string
                  timestamp:
                    description: Timestamp of the collection
                    format: date-time
                    type: string
                  version:
                    description: Collection version identifier
                    type: string
                required:
                - size
                - status
                - timestamp
                - version
                type: object
              lastImport:
                description: Most recent import information
                properties:
                  status:
                    description: Import status
                    type: string
                  timestamp:
                    description: Timestamp of the import
                    format: date-time
                    type: string
                  version:
                    description: Import version identifier
                    type: string
                required:
                - status
                - timestamp
                - version
                type: object
              phase:
                description: Current phase of the platform lifecycle
                type: string
              rhtasRootKeys:
                description: RHTAS root key information for signature verification
                properties:
                  configMap:
                    description: ConfigMap containing the root keys
                    type: string
                  fulcioRootHash:
                    description: Hash of the Fulcio root certificate
                    type: string
                  lastUpdated:
                    description: Timestamp of last root key update
                    format: date-time
                    type: string
                  rekorKeyHash:
                    description: Hash of the Rekor signing key
                    type: string
                  tufRepositoryUrl:
                    description: TUF repository URL for root key distribution
                    type: string
                required:
                - configMap
                - fulcioRootHash
                - lastUpdated
                - rekorKeyHash
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
