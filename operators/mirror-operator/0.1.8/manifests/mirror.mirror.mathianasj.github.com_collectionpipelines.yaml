apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  creationTimestamp: null
  name: collectionpipelines.mirror.mirror.mathianasj.github.com
spec:
  group: mirror.mirror.mathianasj.github.com
  names:
    kind: CollectionPipeline
    listKind: CollectionPipelineList
    plural: collectionpipelines
    shortNames:
    - cp
    singular: collectionpipeline
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              baseVersion:
                type: string
              imageSetConfig:
                type: string
              incremental:
                type: boolean
              parentPipeline:
                description: |-
                  ParentPipeline is the name of a parent CollectionPipeline to inherit cache from.
                  When set, this pipeline will reuse the parent's working PVC for incremental collection.
                type: string
              signing:
                properties:
                  keySecretRef:
                    description: Static key-based signing (legacy)
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  keyless:
                    description: Keyless signing with Fulcio
                    properties:
                      fulcioURL:
                        description: Fulcio URL for certificate issuance
                        type: string
                      oidcClientID:
                        description: OIDC client credentials
                        type: string
                      oidcClientSecret:
                        description: |-
                          LocalObjectReference contains enough information to let you locate the
                          referenced object inside the same namespace.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      oidcIssuer:
                        description: OIDC issuer URL
                        type: string
                      rekorURL:
                        description: Rekor URL for transparency log
                        type: string
                      tufURL:
                        description: TUF URL for root of trust distribution
                        type: string
                    required:
                    - fulcioURL
                    - oidcClientID
                    - oidcIssuer
                    - rekorURL
                    type: object
                  passwordSecretRef:
                    description: |-
                      LocalObjectReference contains enough information to let you locate the
                      referenced object inside the same namespace.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              storage:
                properties:
                  output:
                    properties:
                      filename:
                        type: string
                      pvc:
                        type: string
                      s3:
                        properties:
                          bucket:
                            type: string
                          endpoint:
                            type: string
                          region:
                            type: string
                          secretRef:
                            description: |-
                              LocalObjectReference contains enough information to let you locate the
                              referenced object inside the same namespace.
                            properties:
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                            type: object
                            x-kubernetes-map-type: atomic
                        required:
                        - bucket
                        - region
                        - secretRef
                        type: object
                    type: object
                type: object
              storageSize:
                anyOf:
                - type: integer
                - type: string
                description: StorageSize is the size of the working PVC for collection
                  storage. Defaults to 100Gi.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              timeout:
                description: Timeout for the collection pipeline. Defaults to 12 hours
                  if not specified.
                type: string
              triggerType:
                enum:
                - scheduled
                - manual
                - event
                type: string
            required:
            - imageSetConfig
            type: object
          status:
            properties:
              bundleUrl:
                type: string
              completionTime:
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              configMapRef:
                type: string
              parentPipelineVersion:
                description: |-
                  ParentPipelineVersion is the version of the parent pipeline at the time this pipeline started.
                  Populated from parent's status.version when this pipeline begins.
                type: string
              phase:
                type: string
              pipelineRunRef:
                type: string
              sbomUrl:
                type: string
              signatureUrl:
                type: string
              startTime:
                format: date-time
                type: string
              version:
                type: string
              workingPvcName:
                description: |-
                  WorkingPVCName is the name of the PVC used for working storage during collection.
                  This is the PVC that contains oc-mirror's cache.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
