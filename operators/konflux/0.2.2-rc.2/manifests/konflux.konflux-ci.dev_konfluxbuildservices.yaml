apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: konfluxbuildservices.konflux.konflux-ci.dev
spec:
  group: konflux.konflux-ci.dev
  names:
    kind: KonfluxBuildService
    listKind: KonfluxBuildServiceList
    plural: konfluxbuildservices
    singular: konfluxbuildservice
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KonfluxBuildService is the Schema for the konfluxbuildservices
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            default: {}
            description: KonfluxBuildServiceSpec defines the desired state of KonfluxBuildService
            properties:
              buildControllerManager:
                description: BuildControllerManager defines customizations for the
                  controller-manager deployment.
                properties:
                  manager:
                    description: Manager defines customizations for the manager container.
                    properties:
                      env:
                        description: Env specifies environment variables for the container.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                      resources:
                        description: Resources specifies the resource requirements
                          for the container.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                    type: object
                  replicas:
                    default: 1
                    description: Replicas is the number of replicas for the controller-manager
                      deployment.
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              logEncoder:
                description: |-
                  LogEncoder sets the log encoding format for the build-service controller.
                  "json" produces structured JSON logs (upstream default), "console" produces
                  human-readable output useful for development and debugging.
                  When not set, the upstream default (json) is used.
                enum:
                - json
                - console
                type: string
              pacWebhookInsecureSSL:
                description: |-
                  PACWebhookInsecureSSL controls TLS certificate verification when the build-service
                  configures webhooks on git providers.
                  When set to true, sets PAC_WEBHOOK_INSECURE_SSL=1 on the controller
                  (use only in development or test environments with self-signed certificates).
                  When set to false, forces PAC_WEBHOOK_INSECURE_SSL off, overriding any
                  value in buildControllerManager.manager.env.
                  When omitted, the upstream default applies and PAC_WEBHOOK_INSECURE_SSL
                  in buildControllerManager.manager.env (if set) takes effect.
                type: boolean
              pipelineConfig:
                description: |-
                  PipelineConfig controls the contents of the build-pipeline-config ConfigMap.
                  The operator always manages this ConfigMap; use this field to customize which
                  pipelines are included.

                  Default behavior (nil or empty):
                    The operator applies the full set of default pipeline bundle references.

                  Merge behavior:
                    When set, the operator merges user-specified pipelines with the defaults.
                    Pipelines with matching names override the defaults.
                    Pipelines with removed: true exclude the matching default.
                    Set removeDefaults: true to discard all defaults and use only user-specified pipelines.
                properties:
                  defaultPipelineName:
                    description: |-
                      DefaultPipelineName specifies which pipeline to use as the default.
                      The referenced pipeline must exist in the final merged pipeline list.
                      When not set, the operator-provided default is preserved.
                    maxLength: 253
                    minLength: 1
                    type: string
                  pipelines:
                    description: |-
                      Pipelines specifies user-provided pipeline overrides or additions.
                      Entries with matching names override operator defaults.
                    items:
                      description: PipelineSpec defines a single pipeline entry in
                        the build-pipeline-config ConfigMap.
                      properties:
                        bundle:
                          description: Bundle is the Tekton bundle reference for this
                            pipeline.
                          type: string
                        description:
                          description: |-
                            Description is a human-readable description of this pipeline.
                            When overriding a default pipeline, this replaces the default description.
                          type: string
                        name:
                          description: Name is the pipeline identifier. Must match
                            a default pipeline name to override it.
                          maxLength: 253
                          minLength: 1
                          type: string
                        removed:
                          description: |-
                            Removed excludes this pipeline from the final configuration.
                            Use to remove a specific operator-provided default pipeline.
                            When true, bundle must not be set.
                          type: boolean
                      required:
                      - name
                      type: object
                      x-kubernetes-validations:
                      - message: bundle must not be set when removed is true
                        rule: '!has(self.bundle) || !has(self.removed) || !self.removed'
                      - message: bundle is required when removed is not true
                        rule: (has(self.removed) && self.removed) || (has(self.bundle)
                          && size(self.bundle) > 0)
                    maxItems: 64
                    type: array
                    x-kubernetes-list-type: atomic
                  removeDefaults:
                    description: |-
                      RemoveDefaults disables all operator-provided default pipelines.
                      When true, only user-specified pipelines in the Pipelines list are included.
                    type: boolean
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: defaultPipelineName is required when removeDefaults is
                    true
                  rule: '!has(self.removeDefaults) || !self.removeDefaults || has(self.defaultPipelineName)'
                - message: at least one pipeline with a bundle must be provided when
                    removeDefaults is true
                  rule: '!has(self.removeDefaults) || !self.removeDefaults || self.pipelines.exists(p,
                    !has(p.removed) || !p.removed)'
                - message: defaultPipelineName must reference a pipeline in the pipelines
                    list when removeDefaults is true
                  rule: '!has(self.removeDefaults) || !self.removeDefaults || !has(self.defaultPipelineName)
                    || self.pipelines.exists(p, p.name == self.defaultPipelineName
                    && (!has(p.removed) || !p.removed))'
                - message: defaultPipelineName must not reference a pipeline that
                    is being removed
                  rule: '!has(self.defaultPipelineName) || !self.pipelines.exists(p,
                    p.name == self.defaultPipelineName && has(p.removed) && p.removed)'
              webhookURLs:
                additionalProperties:
                  type: string
                description: |-
                  WebhookURLs maps repository URL prefixes to externally-reachable webhook URLs.
                  When configured, the build-service uses these URLs instead of the default PaC webhook
                  URL when setting up webhooks for git repositories.

                  Keys are matched against the full repository URL using longest-prefix matching.
                  An empty string key ("") serves as a catch-all default for any unmatched repository.

                  Example:
                    webhookURLs:
                      "https://github.com": "https://smee.example.com/github-hook"
                      "https://gitlab.com": "https://smee.example.com/gitlab-hook"
                type: object
            type: object
          status:
            description: KonfluxBuildServiceStatus defines the observed state of KonfluxBuildService
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the KonfluxBuildService state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
        x-kubernetes-validations:
        - message: KonfluxBuildService CR must be named 'konflux-build-service'. Only
            one instance is allowed per cluster.
          rule: self.metadata.name == 'konflux-build-service'
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
